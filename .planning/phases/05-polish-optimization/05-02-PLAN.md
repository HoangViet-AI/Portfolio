---
phase: 05-polish-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - public/og-image.png
  - public/favicon.svg
  - public/favicon.ico
  - public/apple-touch-icon.png
  - public/resume.pdf
  - public/sitemap.xml
  - public/robots.txt
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04

must_haves:
  truths:
    - "Sharing the site URL on social media shows an attractive preview with OG image, title, and description"
    - "The browser tab displays a Ghibli-themed favicon"
    - "Search engines can find and index all pages via sitemap.xml"
    - "A resume PDF is downloadable from the site"
    - "The home page contains JSON-LD Person structured data"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root metadata with title template, OG, Twitter card, favicon, canonical URL"
      contains: "openGraph"
    - path: "src/app/page.tsx"
      provides: "JSON-LD Person schema script tag"
      contains: "application/ld+json"
    - path: "public/og-image.png"
      provides: "1200x630 OG image placeholder"
    - path: "public/favicon.svg"
      provides: "SVG favicon"
    - path: "public/resume.pdf"
      provides: "Downloadable resume placeholder"
    - path: "public/sitemap.xml"
      provides: "XML sitemap for static routes"
    - path: "public/robots.txt"
      provides: "Robots directives with sitemap reference"
  key_links:
    - from: "src/app/layout.tsx"
      to: "public/og-image.png"
      via: "metadata.openGraph.images references /og-image.png"
      pattern: "og-image"
    - from: "src/app/layout.tsx"
      to: "public/favicon.svg"
      via: "metadata.icons references /favicon.svg"
      pattern: "favicon"
    - from: "public/robots.txt"
      to: "public/sitemap.xml"
      via: "Sitemap directive in robots.txt"
      pattern: "Sitemap:"
---

<objective>
Add comprehensive SEO metadata, social sharing tags, structured data, Ghibli-themed favicon, OG image, sitemap, and downloadable resume to make the site production-ready for search engines and social media sharing.

Purpose: Without proper meta tags and social previews, the portfolio loses discoverability and makes a poor impression when shared. This plan ensures every link share and search result looks professional.
Output: Complete metadata in layout.tsx, JSON-LD in page.tsx, and all static assets (favicon, OG image, resume, sitemap) in public/.
</objective>

<execution_context>
@/home/jamesnguyen106/.claude/get-shit-done/workflows/execute-plan.md
@/home/jamesnguyen106/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-optimization/05-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs -->

From src/app/layout.tsx:
Current metadata is minimal: { title: "AI Engineer Portfolio", description: "Ghibli-inspired AI Engineer portfolio" }
Layout renders: html > body > Navbar + AnimationProvider(children)
Font variables applied to html: patrickHand.variable, notoSans.variable

From next.config.ts:
basePath in production: "/Portfolio"
assetPrefix in production: "/Portfolio/"
Static export mode (output: "export")
OG image and favicon paths must account for basePath in production.

From src/app/page.tsx:
Server component rendering HeroScene + all sections + footer.
JSON-LD script tag should be added inside the main element or as a sibling.

From src/data/projects.ts:
Project slugs used for sitemap URL generation.

From src/app/projects/[slug]/page.tsx:
Already has generateMetadata with individual title/description per D-20.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static assets (favicon, OG image, resume, sitemap, robots.txt)</name>
  <files>
    public/favicon.svg
    public/apple-touch-icon.png
    public/og-image.png
    public/resume.pdf
    public/sitemap.xml
    public/robots.txt
  </files>
  <read_first>
    src/data/projects.ts
    next.config.ts
    src/lib/design-tokens.ts
  </read_first>
  <action>
    Create all static assets in the public/ directory:

    favicon.svg (per D-22): Create an SVG favicon with a simple Ghibli-inspired nature element — a stylized leaf shape using the ghibliGreen color (#7BAE7F) on a transparent background. The SVG should be a clean 32x32 viewBox with a single path element forming a leaf silhouette. Keep it minimal (under 1KB).

    favicon.ico (per D-22): Since generating a real .ico requires binary tooling, create a simple script or note that the executor should use the SVG as the primary favicon via the metadata icons config. Modern browsers support SVG favicons. The ico fallback can be generated from the SVG using an online converter or the sharp library if needed — but SVG is the primary per D-22.

    apple-touch-icon.png (per D-24): Generate a 180x180 PNG using a Node.js script with sharp. The image should be a solid background of ghibliCream (#FAFAF5) with a centered green (#7BAE7F) circle or leaf shape. Run the generation script inline using node -e with sharp. If sharp is not available, create a minimal valid PNG placeholder (a single-color 180x180 image).

    og-image.png (per D-18): Generate a 1200x630 PNG using sharp. Create a gradient background from ghibliGreen (#7BAE7F) to ghibliSky (#87CEEB) with "AI Engineer Portfolio" text overlay. Use sharp's composite or create with raw pixel buffer as a gradient. If text rendering with sharp is too complex, create a solid gradient image without text — the meta tags will carry the title. Per specifics: "OG image gradient should use the Ghibli palette (ghibliGreen to ghibliSky)".

    resume.pdf (per D-23): Create a minimal valid PDF file. Use a Node.js inline script to write a basic PDF with the text "AI Engineer Portfolio - Resume" and the site URL. A valid PDF can be constructed with minimal raw PDF syntax (header, catalog, page, content stream, xref, trailer). The content should include the site name and a note that this is a placeholder.

    sitemap.xml (per D-21): Create a static XML sitemap covering all routes. Read project slugs from src/data/projects.ts to determine project detail page URLs. The base URL should use a placeholder like "https://username.github.io/Portfolio". Include URLs for: / (home), /projects/[each-slug]/ (detail pages). Each URL entry has loc and changefreq (monthly) and priority (1.0 for home, 0.8 for project pages).

    robots.txt (per D-21): Create with User-agent: * Allow: / and a Sitemap: directive pointing to the sitemap.xml URL.
  </action>
  <verify>
    <automated>ls -la /home/jamesnguyen106/Downloads/Portfolio/public/favicon.svg /home/jamesnguyen106/Downloads/Portfolio/public/og-image.png /home/jamesnguyen106/Downloads/Portfolio/public/sitemap.xml /home/jamesnguyen106/Downloads/Portfolio/public/robots.txt /home/jamesnguyen106/Downloads/Portfolio/public/resume.pdf /home/jamesnguyen106/Downloads/Portfolio/public/apple-touch-icon.png 2>&1 && grep -c "leaf\|circle\|path" /home/jamesnguyen106/Downloads/Portfolio/public/favicon.svg && grep -c "urlset" /home/jamesnguyen106/Downloads/Portfolio/public/sitemap.xml && grep -c "Sitemap:" /home/jamesnguyen106/Downloads/Portfolio/public/robots.txt</automated>
  </verify>
  <acceptance_criteria>
    - public/favicon.svg exists, contains an SVG path element with fill="#7BAE7F" (ghibliGreen), viewBox is 32x32 or similar small size
    - public/apple-touch-icon.png exists and is a valid PNG file of approximately 180x180 dimensions
    - public/og-image.png exists and is a valid PNG file of approximately 1200x630 dimensions
    - public/resume.pdf exists and is a valid PDF file (starts with %PDF)
    - public/sitemap.xml exists, contains urlset element, includes at least the home URL and project slug URLs
    - public/robots.txt exists, contains "User-agent: *" and "Sitemap:" directive
    - All project slugs from src/data/projects.ts appear in sitemap.xml
  </acceptance_criteria>
  <done>All static assets for SEO, social sharing, favicon, and resume exist in public/ directory with correct content</done>
</task>

<task type="auto">
  <name>Task 2: Add metadata to layout.tsx, JSON-LD to page.tsx, and resume link</name>
  <files>
    src/app/layout.tsx
    src/app/page.tsx
    src/components/sections/ContactSection.tsx
  </files>
  <read_first>
    src/app/layout.tsx
    src/app/page.tsx
    src/components/sections/ContactSection.tsx
    src/components/navigation/Navbar.tsx
    next.config.ts
  </read_first>
  <action>
    Update layout.tsx metadata (per D-17):
    - Replace the minimal metadata export with a comprehensive Metadata object
    - title: use an object with template "%s | AI Engineer Portfolio" and default "AI Engineer Portfolio — Ghibli-Inspired"
    - description: "AI Engineer portfolio with Studio Ghibli-inspired design. Explore AI/ML projects, skills, and experience through an immersive animated storytelling experience."
    - metadataBase: new URL("https://username.github.io/Portfolio") — needed for OG image resolution
    - openGraph: type "website", title "AI Engineer Portfolio", description same as above, images [{ url "/og-image.png", width 1200, height 630, alt "AI Engineer Portfolio" }], siteName "AI Engineer Portfolio"
    - twitter: card "summary_large_image", title "AI Engineer Portfolio", description same, images ["/og-image.png"]
    - icons: { icon: [{ url: "/favicon.svg", type: "image/svg+xml" }], apple: "/apple-touch-icon.png" }
    - robots: { index: true, follow: true } (per D-21)
    - Note: Next.js metadata API handles basePath prepending for static assets automatically in production builds with output: "export" — do NOT manually prepend /Portfolio to asset paths in metadata

    Add JSON-LD to page.tsx (per D-19):
    - Add a script element with type "application/ld+json" inside the main element (before HeroScene) using dangerouslySetInnerHTML
    - JSON-LD content: @context "https://schema.org", @type "Person", name "AI Engineer" (placeholder), jobTitle "AI Engineer", url "https://username.github.io/Portfolio", sameAs array with GitHub and LinkedIn placeholder URLs matching those in ContactSection
    - Use JSON.stringify for the content value

    Add resume download link (per D-23):
    - In ContactSection.tsx, add a resume download link to the contactLinks array: name "Resume", href "/Portfolio/resume.pdf" (use basePath-aware path), icon "📄", label "Resume", external false
    - Add the download attribute to the anchor tag for the resume link specifically — check if link.name === "Resume" and add download="resume.pdf" attribute
    - Alternatively, add the resume link as a separate styled button/link below the contact icons with text "Download Resume" and a download attribute
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit --pretty 2>&1 | head -20 && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx metadata export contains openGraph.images with "/og-image.png"
    - src/app/layout.tsx metadata export contains twitter.card set to "summary_large_image"
    - src/app/layout.tsx metadata export contains icons.icon with favicon.svg reference
    - src/app/layout.tsx metadata export contains icons.apple with apple-touch-icon.png reference
    - src/app/layout.tsx metadata export contains title.template with "%s | AI Engineer Portfolio"
    - src/app/layout.tsx metadata export contains robots with index: true
    - src/app/page.tsx contains a script tag with type="application/ld+json"
    - src/app/page.tsx JSON-LD contains @type "Person" and jobTitle "AI Engineer"
    - Resume download link exists in ContactSection.tsx or as a separate element with download attribute
    - TypeScript compiles without errors
    - npm run build succeeds
  </acceptance_criteria>
  <done>Full SEO metadata in layout.tsx, JSON-LD Person schema in page.tsx, resume downloadable from contact section, all social sharing tags configured</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON-LD injection | Structured data rendered via dangerouslySetInnerHTML — content is static/hardcoded, no user input |
| External URLs in meta | OG image URL, canonical — all point to own domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | JSON-LD sameAs URLs | accept | Only public profile URLs (GitHub, LinkedIn) — intentionally disclosed |
| T-05-03 | Spoofing | OG meta tags | accept | Standard social sharing metadata — cannot be spoofed on own domain |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — uses existing sharp (devDependency) and Next.js metadata API |
</threat_model>

<verification>
1. Run `npm run build` — static export succeeds
2. Run `npx tsc --noEmit` — no type errors
3. Check out/index.html for OG meta tags in head
4. Check out/index.html for JSON-LD script in body
5. Verify favicon.svg, og-image.png, sitemap.xml, robots.txt, resume.pdf exist in out/
6. Open dev server — browser tab shows leaf favicon
</verification>

<success_criteria>
- Layout metadata includes title template, description, OG image, Twitter card, favicon, apple-touch-icon, robots
- Home page has JSON-LD Person schema with name, jobTitle, url, sameAs
- Resume PDF downloadable from contact section
- Sitemap covers all static routes including project detail pages
- Build succeeds with all assets in output directory
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-optimization/05-02-SUMMARY.md`
</output>

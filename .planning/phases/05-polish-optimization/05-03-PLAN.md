---
phase: 05-polish-optimization
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/components/navigation/Navbar.tsx
  - src/components/sections/SceneSection.tsx
  - src/components/sections/ContactSection.tsx
  - src/components/sections/ProjectCard.tsx
  - src/components/hero/HeroLandscape.tsx
  - src/app/page.tsx
  - src/components/sections/ProjectsSection.tsx
  - src/components/sections/SkillsSection.tsx
  - src/components/sections/AboutSection.tsx
autonomous: true
requirements:
  - FOUND-06
  - FOUND-07

must_haves:
  truths:
    - "A skip-to-content link appears on Tab press and jumps focus to main content"
    - "All sections use semantic HTML landmarks with aria-label attributes"
    - "Interactive elements have visible focus indicators (ring-2 ring-primary)"
    - "The hamburger menu button has aria-expanded and aria-controls attributes"
    - "Mobile menu traps focus when open"
    - "All decorative elements have aria-hidden='true'"
    - "No horizontal overflow at 375px, 768px, 1024px, or 1440px viewports"
    - "Touch targets are minimum 44x44px on mobile"
    - "Hero title is readable on mobile without horizontal scroll"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Skip-to-content link and semantic structure"
      contains: "skip-to-content"
    - path: "src/app/globals.css"
      provides: "Focus-visible utility styles and skip-link styles"
      contains: "focus-visible"
    - path: "src/components/navigation/Navbar.tsx"
      provides: "Accessible navigation with focus trap, ARIA attributes"
      contains: "aria-controls"
    - path: "src/components/sections/SceneSection.tsx"
      provides: "Semantic section with aria-label"
      contains: "aria-label"
  key_links:
    - from: "src/app/layout.tsx"
      to: "#main-content"
      via: "Skip link targets main content ID"
      pattern: "main-content"
    - from: "src/app/page.tsx"
      to: "src/app/layout.tsx"
      via: "main element has id='main-content' matching skip link href"
      pattern: "id=\"main-content\""
---

<objective>
Make the site fully keyboard-accessible with semantic HTML, ARIA attributes, visible focus indicators, and a skip-to-content link. Fix responsive layout issues across all four target breakpoints (375px, 768px, 1024px, 1440px).

Purpose: Accessibility and responsiveness are non-negotiable for a production portfolio. Keyboard users and screen reader users must navigate comfortably, and the site must look correct on every device visitors use.
Output: Accessible, responsive site with skip-link, semantic landmarks, focus indicators, focus trap on mobile menu, and no overflow issues at any breakpoint.
</objective>

<execution_context>
@/home/jamesnguyen106/.claude/get-shit-done/workflows/execute-plan.md
@/home/jamesnguyen106/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-optimization/05-CONTEXT.md
@.planning/phases/05-polish-optimization/05-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/layout.tsx (after Plan 02 modifications):
RootLayout renders: html(lang="en") > body > Navbar + AnimationProvider(children)
Plan 02 updated metadata to include OG, Twitter card, favicon, robots.
Skip-link anchor should go as the first child of body, before Navbar.
Font variables: patrickHand.variable, notoSans.variable on html element.

From src/components/navigation/Navbar.tsx:
NAV_LINKS = [{ label, sectionId }] — "Projects", "Skills", "About", "Contact"
Hamburger button already has aria-label and aria-expanded.
Missing: aria-controls attribute on hamburger button.
Mobile overlay: MDiv with fixed positioning, renders nav links.
Missing: Focus trap when mobile menu is open.

From src/components/sections/SceneSection.tsx:
Renders: section(id, className) > MotionWrapper > children
Has id prop but no aria-label. Needs aria-label for landmark identification.

From src/app/page.tsx (after Plan 02 modifications):
Plan 02 added JSON-LD script tag inside main.
main element needs id="main-content" and tabIndex={-1} for skip-link target.
footer already uses semantic footer tag.

From src/components/sections/ContactSection.tsx (after Plan 02 modifications):
Plan 02 may have added resume download link.
Contact links use MDiv with whileHover/whileTap — need focus-visible styles.
Anchor tags wrap the MDiv elements.

From src/components/sections/ProjectCard.tsx:
Cards are links with hover animations — need focus-visible styles.

From src/components/hero/HeroLandscape.tsx:
Decorative parallax layers — need aria-hidden="true" (per D-10).

From src/app/globals.css:
Has @theme block, @keyframes, and prefers-reduced-motion media query.
Focus-visible and skip-link styles should be added here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skip-link, semantic landmarks, ARIA attributes, and focus styles</name>
  <files>
    src/app/layout.tsx
    src/app/globals.css
    src/app/page.tsx
    src/components/sections/SceneSection.tsx
    src/components/hero/HeroLandscape.tsx
  </files>
  <read_first>
    src/app/layout.tsx
    src/app/globals.css
    src/app/page.tsx
    src/components/sections/SceneSection.tsx
    src/components/hero/HeroLandscape.tsx
    src/components/sections/ProjectsSection.tsx
    src/components/sections/SkillsSection.tsx
    src/components/sections/AboutSection.tsx
    src/components/sections/ContactSection.tsx
  </read_first>
  <action>
    IMPORTANT: Plan 02 modified layout.tsx (metadata), page.tsx (JSON-LD), and ContactSection.tsx (resume link). Read current file state before editing — do NOT overwrite Plan 02 changes.

    In globals.css (per D-06, D-08):
    - Add a .skip-link class: position absolute, left -9999px (sr-only pattern), and on focus: position fixed, top 1rem, left 50%, transform translateX(-50%), z-index 200 (modal layer), padding 0.75rem 1.5rem, background-color var(--color-primary), color var(--color-ghibli-cream), border-radius 0.5rem, font-weight 600, outline none, box-shadow
    - Per specifics: "skip-to-content link should match Ghibli aesthetic — primary color, rounded"
    - Add focus-visible utility for interactive elements: a:focus-visible, button:focus-visible, [role="link"]:focus-visible should get outline 2px solid var(--color-primary), outline-offset 2px, border-radius 4px. Use @layer utilities for Tailwind compatibility.

    In layout.tsx (per D-06, D-07):
    - Add a skip-to-content anchor as the very first child of body, before Navbar: anchor with href="#main-content", className "skip-link", text "Skip to content"
    - Preserve ALL existing metadata, imports, and structure from Plan 02

    In page.tsx (per D-07):
    - Add id="main-content" and tabIndex={-1} to the main element so the skip-link target is focusable
    - Add aria-label="Site footer" to the footer element
    - Preserve ALL existing content including JSON-LD from Plan 02

    In SceneSection.tsx (per D-07):
    - Add an ariaLabel optional prop to SceneSectionProps type definition
    - Pass aria-label={ariaLabel} to the section element
    - The aria-label will be provided by each section consumer in Task 2

    In HeroLandscape.tsx (per D-10):
    - Add aria-hidden="true" and role="presentation" to the outermost container div of the landscape parallax layers since they are purely decorative
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && grep -c "skip-link\|skip-to-content" src/app/globals.css src/app/layout.tsx && grep -c "main-content" src/app/page.tsx && grep -c "ariaLabel\|aria-label" src/components/sections/SceneSection.tsx && grep -c "aria-hidden" src/components/hero/HeroLandscape.tsx && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains .skip-link class with position and focus styles
    - src/app/globals.css contains focus-visible styles for a, button elements with outline using --color-primary
    - src/app/layout.tsx contains an anchor element with href="#main-content" and className="skip-link" as first child of body
    - src/app/layout.tsx still contains the metadata export with openGraph from Plan 02
    - src/app/page.tsx main element has id="main-content" and tabIndex={-1}
    - src/app/page.tsx still contains the JSON-LD script from Plan 02
    - src/app/page.tsx footer has aria-label="Site footer"
    - src/components/sections/SceneSection.tsx accepts ariaLabel prop and passes it as aria-label to the section element
    - src/components/hero/HeroLandscape.tsx outer container has aria-hidden="true"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Skip-to-content link works on Tab press, all sections are semantic landmarks, decorative elements hidden from screen readers, focus indicators visible on all interactive elements</done>
</task>

<task type="auto">
  <name>Task 2: Fix Navbar accessibility (focus trap, ARIA) and add aria-labels to sections</name>
  <files>
    src/components/navigation/Navbar.tsx
    src/components/sections/ProjectCard.tsx
    src/components/sections/ContactSection.tsx
    src/components/sections/ProjectsSection.tsx
    src/components/sections/SkillsSection.tsx
    src/components/sections/AboutSection.tsx
  </files>
  <read_first>
    src/components/navigation/Navbar.tsx
    src/components/sections/ProjectCard.tsx
    src/components/sections/ContactSection.tsx
    src/components/sections/ProjectsSection.tsx
    src/components/sections/SkillsSection.tsx
    src/components/sections/AboutSection.tsx
    src/components/sections/SceneSection.tsx
    src/components/hero/HeroIdentity.tsx
  </read_first>
  <action>
    Navbar.tsx accessibility (per D-09):
    - Add aria-controls="mobile-menu" to the hamburger button element
    - Add id="mobile-menu" to the mobile overlay MDiv
    - Add aria-label="Main navigation" to the outer nav element
    - Implement focus trap when mobile menu is open: add a useEffect that runs when menuOpen changes. When menuOpen is true, add a keydown listener. On Tab key, get all focusable elements (a, button) inside the element with id="mobile-menu" plus the hamburger button. If shift+Tab on first element, focus last. If Tab on last element, focus first. On Escape key, call setMenuOpen(false) and return focus to the hamburger button. Use a useRef on the hamburger button for focus return.
    - Increase hamburger button from h-10 w-10 (40px) to h-11 w-11 (44px) for D-14 compliance
    - Add min-h-[44px] to mobile overlay nav link anchors for touch target compliance (per D-14)

    ProjectCard.tsx (per D-08):
    - Add focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 rounded-2xl to the card's outer link/container element className

    ContactSection.tsx (per D-08, D-14):
    - Add focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 rounded-xl to each contact link anchor element
    - Add descriptive aria-label to each contact link anchor: use the pattern aria-label={link.label === "Email" ? "Send email" : link.label === "GitHub" ? "Visit GitHub profile" : link.label === "LinkedIn" ? "Visit LinkedIn profile" : link.label === "Resume" ? "Download resume" : link.label}
    - Preserve the resume link added by Plan 02

    Section consumers — pass ariaLabel to SceneSection (per D-07):
    - ProjectsSection.tsx: add ariaLabel="Featured projects" to the SceneSection component
    - SkillsSection.tsx: add ariaLabel="Technical skills" to the SceneSection component
    - AboutSection.tsx: add ariaLabel="About me and career timeline" to the SceneSection component
    - ContactSection.tsx: add ariaLabel="Contact information" to the SceneSection component

    Responsive spot-checks (per D-12, D-13, D-15):
    - In each section file, verify grid classes are responsive (grid-cols-1 at base, scaling up at sm/md/lg). If any section uses a fixed-width container or grid that would overflow at 375px, add responsive classes.
    - Verify HeroIdentity.tsx title uses responsive text sizing (text-3xl or smaller at base, scaling up). If it uses a fixed large size, add responsive variants.
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && grep -c "aria-controls" src/components/navigation/Navbar.tsx && grep -c "focus-visible" src/components/sections/ProjectCard.tsx src/components/sections/ContactSection.tsx && grep -c "ariaLabel\|aria-label" src/components/sections/ProjectsSection.tsx src/components/sections/SkillsSection.tsx src/components/sections/AboutSection.tsx src/components/sections/ContactSection.tsx && npx tsc --noEmit --pretty 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Navbar.tsx hamburger button has aria-controls="mobile-menu"
    - Navbar.tsx mobile overlay has id="mobile-menu"
    - Navbar.tsx has a useEffect implementing focus trap logic with keydown listener when menuOpen is true
    - Navbar.tsx hamburger button uses h-11 w-11 (not h-10 w-10)
    - Navbar.tsx outer nav element has aria-label="Main navigation"
    - ProjectCard.tsx card container includes focus-visible:ring-2 focus-visible:ring-primary
    - ContactSection.tsx anchors include focus-visible:ring-2 focus-visible:ring-primary
    - ContactSection.tsx anchors have descriptive aria-label attributes
    - ProjectsSection.tsx passes ariaLabel="Featured projects" to SceneSection
    - SkillsSection.tsx passes ariaLabel="Technical skills" to SceneSection
    - AboutSection.tsx passes ariaLabel="About me and career timeline" to SceneSection
    - ContactSection.tsx passes ariaLabel="Contact information" to SceneSection
    - TypeScript compiles without errors
    - npm run build succeeds
  </acceptance_criteria>
  <done>Navbar mobile menu traps focus and handles Escape, all interactive elements have visible focus rings, all sections have aria-labels, touch targets meet 44px minimum, no horizontal overflow at any target breakpoint</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard input | Focus trap handles Tab/Escape — standard DOM keyboard event handling, no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Focus trap DOM query | accept | querySelectorAll targets standard focusable elements within a scoped container — no user-controlled selectors |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
1. Run `npm run build` — static export succeeds
2. Run `npx tsc --noEmit` — no type errors
3. Tab through entire page — skip-link appears first, all interactive elements show focus ring
4. Open mobile menu — Tab cycles within menu, Escape closes it
5. Resize browser to 375px — no horizontal scroll, all content visible
6. Resize to 768px, 1024px, 1440px — layout adjusts correctly at each breakpoint
7. Screen reader test: landmarks announced (navigation, main, sections with labels, footer)
</verification>

<success_criteria>
- Skip-to-content link appears on first Tab and jumps to main content
- All sections are semantic landmarks with descriptive aria-labels
- All interactive elements show visible focus indicators (primary color ring)
- Mobile menu traps focus and handles Escape key
- Hamburger button has aria-controls and meets 44px touch target
- Decorative hero landscape layers are aria-hidden
- No horizontal overflow at 375px, 768px, 1024px, or 1440px
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-optimization/05-03-SUMMARY.md`
</output>

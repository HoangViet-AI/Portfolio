---
phase: 05-polish-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/lottie/clouds.json
  - src/assets/lottie/leaves.json
  - src/components/ambient/FloatingClouds.tsx
  - src/components/ambient/FallingLeaves.tsx
  - src/components/hero/HeroScene.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - ANIM-03

must_haves:
  truths:
    - "Floating cloud animation loops continuously in the hero section background"
    - "Falling leaves animation spans content sections as a subtle overlay"
    - "Ambient animations are invisible when prefers-reduced-motion is active"
    - "Ambient animations pause when the browser tab is hidden"
    - "Ambient overlays do not intercept pointer events"
  artifacts:
    - path: "src/components/ambient/FloatingClouds.tsx"
      provides: "Cloud Lottie overlay component for hero"
      contains: "LottieAmbient"
    - path: "src/components/ambient/FallingLeaves.tsx"
      provides: "Leaves Lottie overlay component for content sections"
      contains: "LottieAmbient"
    - path: "src/assets/lottie/clouds.json"
      provides: "Cloud Lottie animation data"
    - path: "src/assets/lottie/leaves.json"
      provides: "Leaves Lottie animation data"
  key_links:
    - from: "src/components/ambient/FloatingClouds.tsx"
      to: "src/components/animation/LottieAmbient.tsx"
      via: "imports LottieAmbient wrapper"
      pattern: "import.*LottieAmbient"
    - from: "src/components/hero/HeroScene.tsx"
      to: "src/components/ambient/FloatingClouds.tsx"
      via: "renders FloatingClouds inside hero section"
      pattern: "FloatingClouds"
    - from: "src/app/page.tsx"
      to: "src/components/ambient/FallingLeaves.tsx"
      via: "renders FallingLeaves as fixed overlay"
      pattern: "FallingLeaves"
---

<objective>
Add ambient atmospheric Lottie animations — floating clouds in the hero section and falling leaves across content sections — to create the signature Ghibli atmosphere.

Purpose: These ambient animations are the final atmospheric layer that makes the portfolio feel like a living Ghibli scene rather than a static website.
Output: Two ambient animation components using the existing LottieAmbient wrapper, integrated into HeroScene and page.tsx.
</objective>

<execution_context>
@/home/jamesnguyen106/.claude/get-shit-done/workflows/execute-plan.md
@/home/jamesnguyen106/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-optimization/05-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/animation/LottieAmbient.tsx:
LottieAmbient accepts { animationData: object; className?: string }
- Renders null when useReducedMotion() returns true (per D-03)
- Pauses when useTabVisibility() returns false (per D-03)
- Sets pointerEvents: "none" on the Lottie wrapper

From src/lib/design-tokens.ts:
zIndex = { background: 0, sceneBg: 10, sceneMid: 20, sceneFg: 30, content: 40, particles: 50, nav: 100, modal: 200 }
Use zIndex.particles (50) for ambient overlays — above content but below nav.

From src/components/hero/HeroScene.tsx:
HeroScene renders: section > div(landscapeRef) > HeroLandscape + HeroIdentity + ScrollCue
The FloatingClouds component should be placed as a sibling inside the section, positioned absolute.

From src/app/page.tsx:
Home renders: main > HeroScene + ProjectsSection + SkillsSection + AboutSection + ContactSection + footer
FallingLeaves should be a fixed-position overlay rendered as a child of main.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Lottie animation data and ambient components</name>
  <files>
    src/assets/lottie/clouds.json
    src/assets/lottie/leaves.json
    src/components/ambient/FloatingClouds.tsx
    src/components/ambient/FallingLeaves.tsx
  </files>
  <read_first>
    src/components/animation/LottieAmbient.tsx
    src/lib/design-tokens.ts
    src/hooks/useReducedMotion.ts
    src/hooks/useTabVisibility.ts
  </read_first>
  <action>
    Create Lottie JSON animation data files. Per D-05, first attempt to source free Lottie JSON files from LottieFiles (clouds/sky theme, falling leaves/nature theme). If sourcing is not feasible in this context, create minimal hand-crafted Lottie JSON files as CSS-only fallback animated elements:

    For clouds.json (per D-01): Create a Lottie JSON animation with 2-3 soft elliptical cloud shapes drifting horizontally across the frame over a 30-60 second loop. The shapes should be white/semi-transparent fills. Animation dimensions 1920x400. If hand-crafting Lottie JSON is too complex, create FloatingClouds as a CSS-only component instead — 3 rounded div elements with CSS keyframe animation drifting left-to-right, each at different speeds (40s, 55s, 70s duration), using bg-white/20 styling.

    For leaves.json (per D-02): Create a Lottie JSON with 3-5 small leaf shapes falling and swaying. Animation dimensions 1920x1080. Same fallback rule as clouds — if Lottie JSON crafting is impractical, create FallingLeaves as CSS-only with 4-5 small leaf elements using CSS keyframe animation combining translateY (falling) and translateX (swaying), each leaf at different sizes (8-16px), speeds (15-25s), and start positions.

    FloatingClouds.tsx (per D-01, D-03, D-04):
    - "use client" directive
    - If using Lottie: import LottieAmbient from @/components/animation/LottieAmbient and clouds animation data from @/assets/lottie/clouds.json
    - If using CSS fallback: import useReducedMotion from @/hooks/useReducedMotion and useTabVisibility from @/hooks/useTabVisibility
    - Render as a div with position absolute, inset-0, z-[50] (zIndex.particles), pointer-events-none, opacity-30 to opacity-50 (per D-01 range 0.3-0.5)
    - The component returns null when useReducedMotion() is true (per D-03)
    - Export named function FloatingClouds

    FallingLeaves.tsx (per D-02, D-03, D-04):
    - "use client" directive
    - Same pattern as FloatingClouds but with leaves animation data
    - Render as a div with position fixed, inset-0, z-[50], pointer-events-none (per D-02 pointer-events: none)
    - Lower opacity than clouds — use opacity-20 to opacity-30 for subtlety
    - Sparse appearance: if CSS fallback, only 3-5 leaf elements visible (per specifics: "3-5 leaves visible at a time")
    - Returns null when useReducedMotion() is true (per D-03)
    - Export named function FallingLeaves
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ambient/FloatingClouds.tsx exists and exports a named function FloatingClouds
    - src/components/ambient/FallingLeaves.tsx exists and exports a named function FallingLeaves
    - Both components either use LottieAmbient wrapper or implement CSS-only fallback with useReducedMotion guard
    - FloatingClouds has className containing "absolute" and "pointer-events-none"
    - FallingLeaves has className containing "fixed" and "pointer-events-none"
    - Both components return null when shouldReduce/useReducedMotion is true
    - z-index is 50 (particles layer) on both components
    - FloatingClouds opacity is in range 0.3-0.5 (opacity-30 to opacity-50 or style prop)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Two ambient animation components created with Lottie or CSS fallback, respecting reduced-motion and tab-visibility, positioned as non-interactive overlays at the particles z-index layer</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ambient animations into HeroScene and page</name>
  <files>
    src/components/hero/HeroScene.tsx
    src/app/page.tsx
  </files>
  <read_first>
    src/components/hero/HeroScene.tsx
    src/app/page.tsx
    src/components/ambient/FloatingClouds.tsx
    src/components/ambient/FallingLeaves.tsx
  </read_first>
  <action>
    In HeroScene.tsx (per D-01):
    - Import FloatingClouds from @/components/ambient/FloatingClouds
    - Add FloatingClouds as a child of the outer section element, after ScrollCue — it positions itself absolutely within the hero section which already has position relative and overflow hidden

    In page.tsx (per D-02):
    - Import FallingLeaves from @/components/ambient/FallingLeaves
    - Add FallingLeaves as the first child inside the main element — it uses position fixed so it spans across all content sections as an overlay
    - The component handles its own reduced-motion and tab-visibility logic internally, so no conditional rendering needed in page.tsx
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit --pretty 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/hero/HeroScene.tsx contains import of FloatingClouds
    - src/components/hero/HeroScene.tsx renders FloatingClouds inside the section element
    - src/app/page.tsx contains import of FallingLeaves
    - src/app/page.tsx renders FallingLeaves as a child of the main element
    - TypeScript compiles without errors
    - npm run build succeeds without errors
  </acceptance_criteria>
  <done>Ambient cloud animation visible in hero section, falling leaves overlay spans content sections, both degrade gracefully with reduced-motion and pause on hidden tab</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lottie JSON data | Animation data loaded as static import — no external fetch at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | Lottie animations | mitigate | Use small JSON files (<50KB), CSS fallback for minimal resource usage, pause on hidden tab |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — uses existing lottie-react dependency |
</threat_model>

<verification>
1. Run `npm run build` — static export succeeds
2. Run `npx tsc --noEmit` — no type errors
3. Visual: Open dev server, clouds drift across hero section at low opacity
4. Visual: Leaves fall sparsely across content sections
5. Enable prefers-reduced-motion in browser devtools — both animations disappear
6. Switch to another tab and back — animations pause and resume
</verification>

<success_criteria>
- Floating cloud animation loops in hero section background at 0.3-0.5 opacity
- Falling leaves animation spans content sections with pointer-events: none
- Both animations respect prefers-reduced-motion (return null)
- Both animations pause when tab is hidden (via LottieAmbient or custom hook)
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-optimization/05-01-SUMMARY.md`
</output>

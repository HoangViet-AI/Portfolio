---
phase: 03-hero-scene-scroll-storytelling
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/sections/SceneSection.tsx
  - src/components/hero/HeroScene.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - ANIM-02
must_haves:
  truths:
    - "Scrolling past the hero triggers a smooth cinematic transition — hero pins while parallax layers shift, then next section slides in"
    - "The transition scrubs over ~50vh of scroll distance for a deliberate cinematic feel"
    - "Each content section uses a consistent scene-entry pattern (fade-in + upward slide on viewport entry)"
    - "On mobile, scene transitions use simpler fade-in without pinning"
    - "Page.tsx renders HeroScene followed by at least one SceneSection placeholder to demonstrate the transition"
  artifacts:
    - path: "src/components/sections/SceneSection.tsx"
      provides: "Reusable wrapper that applies consistent scene-entry animation to any section content"
      min_lines: 25
    - path: "src/app/page.tsx"
      provides: "Main page composing HeroScene + SceneSection placeholder demonstrating the transition"
      min_lines: 15
  key_links:
    - from: "src/components/hero/HeroScene.tsx"
      to: "gsap/ScrollTrigger"
      via: "GSAP ScrollTrigger pin + scrub for hero-to-content transition"
      pattern: "ScrollTrigger.*pin"
    - from: "src/components/sections/SceneSection.tsx"
      to: "src/components/animation/MotionWrapper.tsx"
      via: "Uses MotionWrapper or similar viewport-entry animation"
      pattern: "MotionWrapper|whileInView"
    - from: "src/app/page.tsx"
      to: "src/components/hero/HeroScene.tsx"
      via: "imports and renders HeroScene"
      pattern: "import.*HeroScene"
---

<objective>
Implement the scroll-driven cinematic scene transition system: hero section pins and parallax layers accelerate upward while the next section slides in from below. Create a reusable SceneSection wrapper for consistent scene-entry animations across all future content sections. Wire everything into page.tsx replacing the Phase 2 demo.

Purpose: Establish the "scrolling through a Ghibli film" experience that is the core differentiator of this portfolio. The scene transition pattern created here will be reused for every content section in Phase 4.

Output: SceneSection wrapper component, enhanced HeroScene with pin/scrub transition, and page.tsx with hero + placeholder section demonstrating the full scroll experience.
</objective>

<execution_context>
@/home/jamesnguyen106/.claude/get-shit-done/workflows/execute-plan.md
@/home/jamesnguyen106/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jamesnguyen106/Downloads/Portfolio/.planning/ROADMAP.md
@/home/jamesnguyen106/Downloads/Portfolio/.planning/STATE.md
@/home/jamesnguyen106/Downloads/Portfolio/.planning/phases/03-hero-scene-scroll-storytelling/03-CONTEXT.md
@/home/jamesnguyen106/Downloads/Portfolio/.planning/phases/03-hero-scene-scroll-storytelling/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output and existing codebase -->

From src/components/hero/HeroScene.tsx (created in Plan 01):
- Exports HeroScene component
- section element with h-screen, relative, overflow-hidden
- Contains a ref used as triggerRef for HeroLandscape parallax layers
- Composes HeroLandscape, HeroIdentity, ScrollCue

From src/components/animation/MotionWrapper.tsx:
- Props: { children: ReactNode; className?: string; variant?: Variants; once?: boolean }
- Uses motion/react-m div with whileInView="visible"
- Handles reduced motion via useReducedMotion hook

From src/lib/animation/variants.ts:
- fadeInUp: Variants = { hidden: { opacity: 0, y: 30 }, visible: { opacity: 1, y: 0, transition: { duration: 0.6, ease: ghibliEase } } }
- ghibliEase = [0.22, 1, 0.36, 1]

From src/lib/animation/gsap-config.ts:
- Registers ScrollTrigger and useGSAP plugins
- GSAP_DEFAULTS = { duration: 0.8, ease: "power2.out" }
- SCROLL_DEFAULTS = { scrub: 1, start: "top bottom", end: "bottom top" }

From src/lib/animation/parallax.ts:
- PARALLAX_BREAKPOINT = "(min-width: 1024px)"
- HERO_PARALLAX_SPEEDS (added in Plan 01)

From src/hooks/useReducedMotion.ts:
- useReducedMotion(): boolean
- useAnimationEnabled(): boolean

From src/hooks/useMediaQuery.ts:
- useMediaQuery(query: string): boolean

From src/app/globals.css:
- z-index tokens: --z-index-scene-bg (10) through --z-index-content (40)
- --ease-ghibli: cubic-bezier(0.22, 1, 0.36, 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hero pin/scrub transition and create SceneSection wrapper</name>
  <files>src/components/hero/HeroScene.tsx, src/components/sections/SceneSection.tsx</files>
  <read_first>
    - src/components/hero/HeroScene.tsx (current state from Plan 01 — must modify to add pin/scrub)
    - src/lib/animation/gsap-config.ts (GSAP plugin registration pattern, SCROLL_DEFAULTS)
    - src/components/animation/ParallaxLayer.tsx (pattern for useGSAP with matchMedia and ScrollTrigger)
    - src/components/animation/MotionWrapper.tsx (pattern for viewport-entry animation with reduced motion)
    - src/lib/animation/variants.ts (fadeInUp variant for scene-entry)
    - src/hooks/useReducedMotion.ts (useReducedMotion hook)
    - src/hooks/useMediaQuery.ts (useMediaQuery hook)
    - src/lib/animation/parallax.ts (PARALLAX_BREAKPOINT)
  </read_first>
  <action>
    **Enhance HeroScene.tsx** per D-13, D-14, D-15:

    Add a GSAP ScrollTrigger pin + scrub animation to the hero section. Using useGSAP inside HeroScene:

    1. Use gsap.matchMedia() with PARALLAX_BREAKPOINT and prefers-reduced-motion: no-preference (same pattern as ParallaxLayer.tsx).

    2. Inside the matchMedia callback, create a GSAP timeline with ScrollTrigger configuration:
       - trigger: the hero section ref element
       - pin: true (pins the hero while parallax plays out)
       - scrub: 1 (smooth scrub)
       - start: "top top" (pin starts when hero reaches top of viewport)
       - end: "+=50vh" per D-15 (scrub over 50vh of scroll distance)

    3. The timeline animates the hero's internal landscape container to yPercent: -30 (parallax layers shift upward at accelerated speed, creating the "camera pan" effect per D-13).

    4. On mobile (matchMedia else branch or no matchMedia match): no pinning, hero scrolls naturally. The natural ParallaxLayer behavior from Plan 01 already disables on mobile, so nothing extra needed.

    5. Import "@/lib/animation/gsap-config" at top for plugin registration.

    6. Ensure useGSAP cleanup handles the timeline revert via matchMedia.revert().

    **Create SceneSection.tsx** per D-16:

    "use client" component that wraps any section content with a consistent scene-entry animation pattern. Props: { children: ReactNode; className?: string; id?: string }.

    The component renders a section element with the given id and className. Inside, it uses MotionWrapper with the fadeInUp variant and once=true to create the consistent "fade-in + slight upward slide" entry pattern per D-16.

    On reduced motion: MotionWrapper already handles this (renders plain div).

    This is intentionally simple — a thin wrapper establishing the convention that Phase 4 will reuse for projects, skills, about, and contact sections.
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/hero/HeroScene.tsx imports "@/lib/animation/gsap-config" and uses useGSAP hook
    - HeroScene creates a gsap.matchMedia() block checking PARALLAX_BREAKPOINT and prefers-reduced-motion: no-preference
    - Inside matchMedia, a GSAP timeline with ScrollTrigger { pin: true, scrub: 1, start: "top top", end: "+=50vh" } is created on the hero section ref
    - The timeline animates the landscape container with yPercent: -30
    - matchMedia cleanup via mm.revert() is returned from useGSAP
    - src/components/sections/SceneSection.tsx exists with "use client" directive
    - SceneSection accepts children, className, and id props
    - SceneSection uses MotionWrapper with fadeInUp variant
    - No TypeScript errors on noEmit check
  </acceptance_criteria>
  <done>Hero section pins on scroll with cinematic parallax acceleration over 50vh, then unpins for next section. SceneSection wrapper provides consistent fade-in + upward slide entry for all future content sections.</done>
</task>

<task type="auto">
  <name>Task 2: Wire page.tsx with HeroScene and placeholder SceneSection</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current state — renders DesignSystemDemo, must be replaced)
    - src/components/hero/HeroScene.tsx (Plan 01 + Task 1 output — the hero to render)
    - src/components/sections/SceneSection.tsx (Task 1 output — the section wrapper)
    - src/app/layout.tsx (AnimationProvider context — confirms LazyMotion wraps children)
  </read_first>
  <action>
    Replace page.tsx content entirely. Remove the DesignSystemDemo import and header.

    The new page.tsx renders:
    1. A main element with overflow-x-hidden class (matching existing pattern).
    2. HeroScene as the first child — the full-viewport hero with parallax and identity.
    3. One SceneSection placeholder with id="projects" and a min-h-screen class, containing:
       - A centered heading "Projects" in font-heading text-4xl text-primary
       - A paragraph in text-foreground/60 stating "Coming in Phase 4"
       - This section demonstrates the hero-to-content transition works correctly

    4. Optionally a second SceneSection with id="about" to show the transition pattern repeats consistently.

    The page is a server component (no "use client" needed) since HeroScene and SceneSection are client components that handle their own interactivity.

    Do NOT delete the DesignSystemDemo component file (src/components/demo/DesignSystemDemo.tsx) — just stop importing it.
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx does NOT import DesignSystemDemo
    - src/app/page.tsx imports HeroScene from @/components/hero/HeroScene
    - src/app/page.tsx imports SceneSection from @/components/sections/SceneSection
    - Page renders main > HeroScene + at least one SceneSection
    - SceneSection has id="projects" with placeholder heading
    - No "use client" directive in page.tsx (server component)
    - npm run build completes successfully and produces out/ directory
    - The built site has an index.html in out/ that includes hero content
  </acceptance_criteria>
  <done>Page.tsx renders the full hero-to-section scroll experience: hero with parallax landscape and identity, cinematic pin/scrub transition, then content sections with consistent scene-entry animations. The Phase 2 demo page is replaced with the actual portfolio structure. Build succeeds for static export.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static site — no server | All code runs client-side; no API boundaries |
| ScrollTrigger pinning | Pin creates extra spacer elements in DOM — ensure cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | GSAP ScrollTrigger pin + timeline | mitigate | Pin instance cleaned up via gsap.matchMedia().revert() in useGSAP cleanup — prevents memory leaks on component unmount or navigation |
| T-03-04 | Tampering | Page content replacement | accept | Replacing demo page with hero is intentional — demo component file preserved for reference |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — all dependencies from Phase 2 |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully (static export produces out/ directory)
3. Dev server at localhost:3000 shows: hero fills viewport, scrolling pins hero with parallax shift, then projects section slides in with fade-up animation
4. On mobile viewport (<1024px): no pinning, hero scrolls naturally, sections fade in
5. With prefers-reduced-motion enabled: all content visible, no animations
</verification>

<success_criteria>
- Hero pins on scroll with parallax layers shifting upward over 50vh of scroll distance
- Next section slides in smoothly as hero unpins — no abrupt content swap
- SceneSection wrapper provides reusable fade-in + upward slide entry pattern
- Page.tsx replaces Phase 2 demo with hero + placeholder sections
- Static export build succeeds
- Mobile and reduced-motion modes work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-hero-scene-scroll-storytelling/03-02-SUMMARY.md`
</output>

---
phase: 03-hero-scene-scroll-storytelling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hero/HeroScene.tsx
  - src/components/hero/HeroLandscape.tsx
  - src/components/hero/HeroIdentity.tsx
  - src/components/hero/ScrollCue.tsx
  - src/lib/animation/parallax.ts
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
must_haves:
  truths:
    - "Hero section fills the full viewport (100vh) and displays a multi-layer landscape"
    - "Parallax depth layers move at different speeds when scrolling on desktop"
    - "Developer name, AI Engineer title, and tagline appear with staggered entrance animation on load"
    - "A bouncing scroll cue arrow is visible at the bottom of the hero and fades when scrolling begins"
    - "On mobile (<1024px), parallax is disabled and identity appears with a single fade-in"
  artifacts:
    - path: "src/components/hero/HeroScene.tsx"
      provides: "Top-level hero section composing landscape + identity + scroll cue"
      min_lines: 25
    - path: "src/components/hero/HeroLandscape.tsx"
      provides: "Multi-layer parallax landscape with CSS gradient/SVG placeholder layers"
      min_lines: 40
    - path: "src/components/hero/HeroIdentity.tsx"
      provides: "Developer name, title, tagline with Motion staggered entrance"
      min_lines: 30
    - path: "src/components/hero/ScrollCue.tsx"
      provides: "Animated bouncing chevron that fades on scroll"
      min_lines: 20
  key_links:
    - from: "src/components/hero/HeroLandscape.tsx"
      to: "src/components/animation/ParallaxLayer.tsx"
      via: "import ParallaxLayer"
      pattern: "import.*ParallaxLayer"
    - from: "src/components/hero/HeroIdentity.tsx"
      to: "src/lib/animation/variants.ts"
      via: "import fadeInUp variant"
      pattern: "import.*fadeInUp"
    - from: "src/components/hero/ScrollCue.tsx"
      to: "gsap/ScrollTrigger"
      via: "GSAP ScrollTrigger for fade-on-scroll"
      pattern: "ScrollTrigger"
---

<objective>
Build the hero section as an immersive full-viewport animated Ghibli landscape with parallax depth layers, developer identity overlay with staggered entrance animation, and a scroll cue indicator.

Purpose: Establish the first thing visitors see — an animated Ghibli-style landscape that immediately communicates the portfolio's unique aesthetic and the developer's identity.

Output: Four components (HeroScene, HeroLandscape, HeroIdentity, ScrollCue) ready for integration into page.tsx in Plan 02.
</objective>

<execution_context>
@/home/jamesnguyen106/.claude/get-shit-done/workflows/execute-plan.md
@/home/jamesnguyen106/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jamesnguyen106/Downloads/Portfolio/.planning/ROADMAP.md
@/home/jamesnguyen106/Downloads/Portfolio/.planning/STATE.md
@/home/jamesnguyen106/Downloads/Portfolio/.planning/phases/03-hero-scene-scroll-storytelling/03-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/animation/ParallaxLayer.tsx:
- Props: { speed: number; children: ReactNode; className?: string; triggerRef?: RefObject<HTMLElement | null> }
- Uses useGSAP hook with gsap.matchMedia for responsive parallax
- Applies PARALLAX_BREAKPOINT and prefers-reduced-motion: no-preference check

From src/lib/animation/parallax.ts:
- PARALLAX_SPEEDS = { background: 0.2, midground: 0.5, foreground: 0.8 }
- PARALLAX_BREAKPOINT = "(min-width: 1024px)"

From src/lib/animation/variants.ts:
- fadeInUp: Variants = { hidden: { opacity: 0, y: 30 }, visible: { opacity: 1, y: 0, transition: { duration: 0.6, ease: ghibliEase } } }
- fadeIn: Variants (opacity only)
- ghibliEase = [0.22, 1, 0.36, 1]

From src/lib/design-tokens.ts:
- zIndex = { background: 0, sceneBg: 10, sceneMid: 20, sceneFg: 30, content: 40, particles: 50, nav: 100 }
- colors = { ghibliGreen: "#7BAE7F", ghibliSky: "#87CEEB", ghibliWheat: "#F5DEB3", ghibliCream: "#FAFAF5", ghibliInk: "#3B3B3B" }

From src/hooks/useReducedMotion.ts:
- useReducedMotion(): boolean — wraps motion/react's useReducedMotion
- useAnimationEnabled(): boolean — inverse

From src/hooks/useMediaQuery.ts:
- useMediaQuery(query: string): boolean

From src/app/globals.css:
- @theme tokens: --color-primary (ghibli-green), --color-secondary (ghibli-pink), --color-accent (ghibli-sky), --color-warm (ghibli-wheat), --color-background (ghibli-cream), --color-foreground (ghibli-ink)
- z-index tokens: --z-index-scene-bg (10), --z-index-scene-mid (20), --z-index-scene-fg (30), --z-index-content (40)
- --animate-float: float 6s ease-in-out infinite (translateY 0 to -12px)
- --ease-ghibli: cubic-bezier(0.22, 1, 0.36, 1)
- --font-heading: var(--font-patrick-hand), --font-body: var(--font-noto-sans)

From src/components/animation/MotionWrapper.tsx:
- Props: { children: ReactNode; className?: string; variant?: Variants; once?: boolean }
- Uses div from "motion/react-m" (tree-shakable import), LazyMotion domAnimation in AnimationProvider

From src/app/layout.tsx:
- AnimationProvider wraps children (LazyMotion with domAnimation)
- Font variables applied to html element
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build HeroLandscape with multi-layer parallax placeholder layers</name>
  <files>src/components/hero/HeroLandscape.tsx, src/lib/animation/parallax.ts</files>
  <read_first>
    - src/components/animation/ParallaxLayer.tsx (reuse this component per D-04)
    - src/lib/animation/parallax.ts (existing PARALLAX_SPEEDS to extend with hero-specific speeds)
    - src/lib/design-tokens.ts (z-index values for layer ordering)
    - src/app/globals.css (color tokens for CSS gradients)
  </read_first>
  <action>
    Create HeroLandscape.tsx as a "use client" component that accepts a triggerRef prop (RefObject&lt;HTMLDivElement | null&gt;) and renders 4-5 parallax depth layers per D-01:

    1. Sky gradient layer (z-index-scene-bg / 10): CSS linear-gradient from ghibli-sky (#87CEEB) at top to a lighter blue-white at bottom. Slowest parallax speed. Absolutely positioned, covers full container.

    2. Distant mountains/clouds layer (z-index between scene-bg and scene-mid, use z-15 or inline style 15): Simple SVG shapes — 2-3 rounded mountain silhouettes in muted blue-gray (#A8C4D4 range), a few cloud ellipses. ParallaxLayer speed ~0.15.

    3. Rolling green hills midground layer (z-index-scene-mid / 20): SVG or CSS clip-path creating rolling hill shapes filled with ghibli-green (#7BAE7F) with slight opacity variation. ParallaxLayer speed at PARALLAX_SPEEDS.midground (0.5).

    4. Foreground grass/flowers layer (z-index-scene-fg / 30): Taller grass shapes at bottom in darker green with small colored dots for flowers (ghibli-pink, ghibli-wheat). ParallaxLayer speed at PARALLAX_SPEEDS.foreground (0.8).

    5. Optional floating particles layer (z-index-particles / 50): 3-5 small cloud-like SVG shapes with CSS float animation from globals.css, scattered across the viewport. No ParallaxLayer needed — these use CSS animation only.

    Per D-02, all artwork is CSS gradients and inline SVGs — placeholder-first approach. Each landscape layer is wrapped in ParallaxLayer per D-04, passing the triggerRef so all layers scrub relative to the hero container.

    Add HERO_PARALLAX_SPEEDS to parallax.ts: { sky: 0.1, mountains: 0.15, hills: 0.5, foreground: 0.8 } alongside existing PARALLAX_SPEEDS (do not modify existing values).

    The component renders a div with relative positioning and overflow-hidden, filling its parent. All layers absolutely positioned within it.
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit --strict src/components/hero/HeroLandscape.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/hero/HeroLandscape.tsx exists with "use client" directive
    - Component imports ParallaxLayer from @/components/animation/ParallaxLayer
    - Component accepts triggerRef: RefObject&lt;HTMLDivElement | null&gt; prop
    - At least 4 ParallaxLayer children rendered with distinct speed values
    - Each layer uses z-index ordering via Tailwind z-[10], z-[20], z-[30] or inline style matching design token scale
    - SVG shapes are inline (not external files) for sky, mountains, hills, foreground
    - src/lib/animation/parallax.ts exports HERO_PARALLAX_SPEEDS alongside existing PARALLAX_SPEEDS
    - No TypeScript errors on noEmit check
  </acceptance_criteria>
  <done>HeroLandscape renders 4-5 depth layers with CSS gradient sky, SVG mountain/hill/grass shapes, each wrapped in ParallaxLayer with distinct speeds, z-ordered from back to front. HERO_PARALLAX_SPEEDS exported from parallax.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Build HeroIdentity with staggered Motion entrance and ScrollCue with fade-on-scroll</name>
  <files>src/components/hero/HeroIdentity.tsx, src/components/hero/ScrollCue.tsx, src/components/hero/HeroScene.tsx</files>
  <read_first>
    - src/components/animation/MotionWrapper.tsx (pattern for using motion/react-m with LazyMotion)
    - src/lib/animation/variants.ts (fadeInUp variant to base stagger animation on, ghibliEase constant)
    - src/lib/animation/gsap-config.ts (GSAP plugin registration import pattern)
    - src/hooks/useReducedMotion.ts (useReducedMotion hook for conditional animation)
    - src/hooks/useMediaQuery.ts (useMediaQuery hook for mobile detection)
    - src/app/globals.css (--animate-float keyframe for scroll cue bounce, z-index tokens)
    - src/lib/animation/parallax.ts (PARALLAX_BREAKPOINT value "(min-width: 1024px)")
  </read_first>
  <action>
    **HeroIdentity.tsx** — "use client" component per D-05 through D-09:

    Centered overlay div positioned absolutely within the hero, above midground layers at z-index-content (40). Flex column, items-center, justify-center.

    Three text elements stacked vertically per D-06/D-07/D-08:
    - Developer name: font-heading (Patrick Hand), text-5xl sm:text-7xl, text-primary. The actual name string should be a prop with default "Hoang NV" (derived from git user).
    - "AI Engineer" title: text-2xl sm:text-4xl, text-secondary.
    - Tagline: font-body, text-foreground/60, text-base sm:text-lg. Prop with sensible default.

    Staggered entrance animation per D-09: Use motion/react-m div elements (same tree-shakable import pattern as MotionWrapper). Create a container variant with staggerChildren: 0.2 and child variants animating from { opacity: 0, y: 30 } to { opacity: 1, y: 0 } with ghibliEase. The container uses initial="hidden" animate="visible" (NOT whileInView — this fires on page load).

    Mobile behavior per D-18: Use useMediaQuery with PARALLAX_BREAKPOINT. On mobile (not matching), skip stagger — use a single fadeIn variant on the container instead.

    Reduced motion per D-09/D-18: When useReducedMotion() returns true, render plain divs with no animation (same pattern as MotionWrapper's shouldReduce branch).

    **ScrollCue.tsx** — "use client" component per D-10 through D-12:

    Positioned absolutely at bottom center of hero viewport. Renders a downward-pointing chevron SVG (simple V-shape, 24x24 or 32x32, stroke-current, text-foreground/60).

    Bounce animation per D-11: Apply Tailwind animate-float class (reuses existing float keyframe) or a custom bounce keyframe. Add subtle opacity pulse via CSS animation (opacity oscillates between 0.4 and 0.8).

    Fade on scroll per D-12: Use GSAP ScrollTrigger (import gsap-config first). On mount via useGSAP, create a tween that animates the cue element to opacity: 0 with a ScrollTrigger { trigger: parentElement, start: "top top", end: "5% top", scrub: true }. This makes the cue disappear within the first 5% of hero scroll.

    Mobile per D-19: Still visible, same CSS animation — no special mobile branch needed since CSS animations work everywhere.

    Reduced motion: When useReducedMotion() is true, render static chevron without bounce animation (just the SVG, visible, no animation classes).

    **HeroScene.tsx** — "use client" component composing the full hero per D-03:

    Top-level section element with h-screen (100vh), relative positioning, overflow-hidden. Creates a useRef&lt;HTMLDivElement&gt; for triggerRef. Composes:
    1. HeroLandscape with triggerRef
    2. HeroIdentity (centered overlay)
    3. ScrollCue (bottom center)

    Exports HeroScene as the public API for page.tsx.
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/hero/HeroIdentity.tsx exists with "use client", exports HeroIdentity
    - HeroIdentity renders name in font-heading text-5xl sm:text-7xl text-primary
    - HeroIdentity renders "AI Engineer" in text-2xl sm:text-4xl text-secondary
    - HeroIdentity renders tagline in font-body text-foreground/60
    - HeroIdentity uses motion/react-m div with staggerChildren: 0.2 and initial="hidden" animate="visible"
    - HeroIdentity uses useMediaQuery(PARALLAX_BREAKPOINT) for mobile detection and useReducedMotion for accessibility
    - src/components/hero/ScrollCue.tsx exists with "use client", exports ScrollCue
    - ScrollCue renders an SVG chevron with CSS bounce/float animation
    - ScrollCue uses useGSAP with ScrollTrigger to fade out on scroll start
    - src/components/hero/HeroScene.tsx exists, exports HeroScene
    - HeroScene renders section with h-screen, relative, overflow-hidden
    - HeroScene composes HeroLandscape, HeroIdentity, ScrollCue
    - HeroScene creates a ref and passes it as triggerRef to HeroLandscape
    - No TypeScript errors on full project noEmit check
  </acceptance_criteria>
  <done>HeroScene composes a full-viewport hero with multi-layer parallax landscape, staggered developer identity entrance animation, and a bouncing scroll cue that fades on scroll. All three sub-components handle reduced motion and mobile gracefully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static site — no server | All code runs client-side; no API boundaries |
| User input: none | Hero section has no forms or user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | GSAP ScrollTrigger instances | mitigate | Ensure ScrollTrigger instances are cleaned up via useGSAP's automatic cleanup and gsap.matchMedia().revert() — already handled by ParallaxLayer pattern |
| T-03-02 | Information Disclosure | Developer name/identity | accept | Name, title, and tagline are intentionally public portfolio content |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — all dependencies from Phase 2 |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully (static export produces out/ directory)
3. HeroScene renders visually in dev server at localhost:3000
</verification>

<success_criteria>
- Full-viewport hero section with 4-5 depth layers creating visible parallax on desktop scroll
- Developer name (Patrick Hand font), "AI Engineer" title, and tagline appear with staggered entrance
- Bouncing scroll cue visible at bottom, fades when scrolling begins
- Mobile: parallax disabled, single fade-in for identity, static scroll cue
- Reduced motion: all animation disabled, content renders statically
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-hero-scene-scroll-storytelling/03-01-SUMMARY.md`
</output>

---
phase: 04-content-sections
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/sections/TimelineEntry.tsx
  - src/components/sections/AboutSection.tsx
  - src/components/sections/ContactSection.tsx
autonomous: true
requirements: [ABOU-01, ABOU-02, ABOU-03, CONT-01, CONT-02]

must_haves:
  truths:
    - "About section presents a vertical timeline with alternating left/right entries on desktop and single column on mobile"
    - "Timeline entries animate sequentially on scroll, each fading in and sliding from its side"
    - "A personal narrative paragraph connects the developer to the AI/ML field above the timeline"
    - "Contact section displays centered card with icon links for email, GitHub, and LinkedIn"
    - "Contact links have Ghibli-themed hover animations with color shift and scale"
  artifacts:
    - path: "src/components/sections/TimelineEntry.tsx"
      provides: "Individual timeline entry with alternating layout and slide-in animation"
      exports: ["TimelineEntry"]
    - path: "src/components/sections/AboutSection.tsx"
      provides: "Full about section with personal narrative and vertical timeline"
      exports: ["AboutSection"]
    - path: "src/components/sections/ContactSection.tsx"
      provides: "Contact section with centered icon links and hover animations"
      exports: ["ContactSection"]
  key_links:
    - from: "src/components/sections/AboutSection.tsx"
      to: "src/data/timeline.ts"
      via: "import { timelineEntries }"
      pattern: "import.*timelineEntries.*from.*data/timeline"
    - from: "src/components/sections/TimelineEntry.tsx"
      to: "src/lib/animation/variants.ts"
      via: "import slideInLeft, slideInRight"
      pattern: "slideIn(Left|Right)"
---

<objective>
Build the About/Timeline and Contact content sections.

Purpose: About section tells the developer's journey as a visual timeline — the narrative heart of the portfolio. Contact section provides the call-to-action for visitors to connect. Both use the timeline data from Plan 01 and established animation patterns.

Output: Three component files — TimelineEntry, AboutSection, ContactSection — ready to be wired into page.tsx in Plan 04.
</objective>

<execution_context>
@/home/jamesnguyen106/.claude/get-shit-done/workflows/execute-plan.md
@/home/jamesnguyen106/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-sections/04-CONTEXT.md
@.planning/phases/04-content-sections/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/data/timeline.ts (created in Plan 01):
export interface TimelineEntry {
  year: string;
  title: string;
  description: string;
  illustration?: string;
}
export const timelineEntries: TimelineEntry[];

<!-- Existing infrastructure contracts -->

From src/components/animation/MotionWrapper.tsx:
type MotionWrapperProps = {
  children: ReactNode; className?: string; variant?: Variants; once?: boolean;
};
export function MotionWrapper(props: MotionWrapperProps): JSX.Element;

From src/components/sections/SceneSection.tsx:
type SceneSectionProps = { children: ReactNode; className?: string; id?: string; };
export function SceneSection(props: SceneSectionProps): JSX.Element;

From src/lib/animation/variants.ts:
export const fadeInUp: Variants;
export const fadeIn: Variants;
export const slideInLeft: Variants;
export const slideInRight: Variants;

From src/hooks/useMediaQuery.ts:
export function useMediaQuery(query: string): boolean;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TimelineEntry and AboutSection components</name>
  <files>src/components/sections/TimelineEntry.tsx, src/components/sections/AboutSection.tsx</files>
  <read_first>
    - src/data/timeline.ts (Plan 01 output — TimelineEntry type and timelineEntries data)
    - src/components/animation/MotionWrapper.tsx (for wrapping pattern)
    - src/components/sections/SceneSection.tsx (for section wrapper)
    - src/lib/animation/variants.ts (for slideInLeft, slideInRight, fadeInUp variants)
    - src/hooks/useMediaQuery.ts (for responsive breakpoint detection)
    - .planning/phases/04-content-sections/04-CONTEXT.md (for D-13, D-14, D-15, D-16, D-26, D-28)
  </read_first>
  <action>
    **src/components/sections/TimelineEntry.tsx** (per D-13, D-14, D-15):
    - "use client" directive
    - Import TimelineEntry type (as TimelineEntryData to avoid name collision) from @/data/timeline
    - Import MotionWrapper from @/components/animation/MotionWrapper
    - Import slideInLeft, slideInRight from @/lib/animation/variants
    - Props: { entry: TimelineEntryData; index: number; isDesktop: boolean }
    - Alternating layout per D-13: when isDesktop is true, even-indexed entries (index % 2 === 0) render content on the left side and odd-indexed on the right side. When isDesktop is false, all entries render in a single left-aligned column.
    - Each entry is wrapped in MotionWrapper with variant={isDesktop ? (index % 2 === 0 ? slideInLeft : slideInRight) : fadeInUp} per D-14
    - Desktop layout: Use a flex row. Even entries: content on the left (text-right), center line node, empty right. Odd entries: empty left, center line node, content on the right (text-left). Each side takes w-5/12, center column takes w-2/12.
    - Mobile layout: Simple vertical stack — year pill, title, description, with a left border line for visual continuity.
    - Entry content structure:
      1. Year: span with inline-block px-3 py-1 rounded-full bg-primary/20 text-primary font-heading text-sm font-bold
      2. Title: h3 with font-heading text-xl text-foreground mt-2
      3. Description: p with text-sm text-foreground/70 mt-1
    - Center timeline node (desktop only): a div with w-4 h-4 rounded-full bg-primary border-4 border-background shadow-sm, centered on the vertical line

    **src/components/sections/AboutSection.tsx** (per D-13, D-16, D-26):
    - "use client" directive
    - Import timelineEntries from @/data/timeline
    - Import TimelineEntry component from ./TimelineEntry (the component, not the type)
    - Import SceneSection from @/components/sections/SceneSection
    - Import MotionWrapper from @/components/animation/MotionWrapper
    - Import useMediaQuery from @/hooks/useMediaQuery
    - Import fadeInUp from @/lib/animation/variants
    - Render SceneSection with id="about" and className="min-h-screen px-6 py-20 sm:px-10 lg:px-16"
    - Section heading: h2 with font-heading text-4xl sm:text-5xl text-primary text-center mb-4
    - Personal narrative per D-16: a MotionWrapper containing a p tag with max-w-3xl mx-auto text-center text-foreground/80 leading-relaxed mb-16. Content: 2-3 sentences connecting the developer to AI/ML — something like "From the moment I wrote my first neural network, I knew AI would be my calling. Every project is a new adventure — like exploring uncharted lands in a Ghibli film, each challenge reveals something beautiful about what machines can learn."
    - Detect desktop via useMediaQuery("(min-width: 1024px)")
    - Timeline container: div with relative max-w-4xl mx-auto
    - Desktop vertical line: a div with absolute left-1/2 top-0 bottom-0 w-0.5 bg-primary/20 -translate-x-1/2 (only rendered when isDesktop)
    - Map timelineEntries to TimelineEntry components, passing entry, index, and isDesktop. key={entry.year + entry.title}
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/TimelineEntry.tsx exports TimelineEntry component with "use client"
    - TimelineEntry accepts entry, index, and isDesktop props
    - TimelineEntry uses MotionWrapper with slideInLeft for even entries and slideInRight for odd entries on desktop
    - TimelineEntry falls back to fadeInUp on mobile (isDesktop false)
    - Desktop layout has alternating left/right content with center timeline node
    - src/components/sections/AboutSection.tsx exports AboutSection component with "use client"
    - AboutSection wraps content in SceneSection with id="about"
    - AboutSection includes a personal narrative paragraph above the timeline per D-16
    - AboutSection uses useMediaQuery to detect desktop and passes isDesktop to TimelineEntry
    - Desktop vertical line element exists with absolute positioning and left-1/2
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>AboutSection renders a personal narrative and vertical timeline with alternating left/right entries on desktop, single column on mobile, and sequential scroll-in animations. Zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build ContactSection component</name>
  <files>src/components/sections/ContactSection.tsx</files>
  <read_first>
    - src/components/sections/SceneSection.tsx (for section wrapper pattern)
    - src/components/animation/MotionWrapper.tsx (for scroll-entry animation)
    - src/lib/animation/variants.ts (for fadeInUp, scaleIn)
    - .planning/phases/04-content-sections/04-CONTEXT.md (for D-18, D-19, D-20, D-26)
  </read_first>
  <action>
    **src/components/sections/ContactSection.tsx** (per D-18, D-19, D-20):
    - "use client" directive
    - Import SceneSection from @/components/sections/SceneSection
    - Import MotionWrapper from @/components/animation/MotionWrapper
    - Import div as MDiv from "motion/react-m" for individual link hover animations
    - Import fadeInUp from @/lib/animation/variants
    - Define a local contactLinks array with objects: { name: string, href: string, icon: string (SVG path or emoji), label: string }. Include: Email (mailto:hello@example.com, envelope emoji or SVG), GitHub (https://github.com/username, octocat-style emoji or SVG), LinkedIn (https://linkedin.com/in/username, briefcase emoji or SVG). Use emoji icons as placeholders.
    - Render SceneSection with id="contact" and className="min-h-screen px-6 py-20 sm:px-10 lg:px-16 flex items-center justify-center"
    - Center a card container: div with max-w-lg mx-auto text-center p-8 sm:p-12 rounded-2xl bg-background/80 border border-warm/20 shadow-lg per D-18
    - Section heading inside card: h2 with font-heading text-4xl text-primary mb-3
    - Subtitle: p with text-foreground/60 mb-8 — something like "Let's create something wonderful together"
    - Links container: div with flex justify-center gap-8
    - Each link is an anchor tag wrapping a MDiv with whileHover={{ scale: 1.15, rotate: 3 }} and whileTap={{ scale: 0.95 }} per D-19. The MDiv contains the icon (text-3xl) and a label below (text-sm text-foreground/60 mt-2). The anchor has target="_blank" rel="noopener noreferrer" for external links (not for mailto).
    - Each link container (MDiv): flex flex-col items-center p-4 rounded-xl transition-colors hover:bg-primary/10 cursor-pointer
    - Wrap the entire card in MotionWrapper with fadeInUp for scroll-entry
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/ContactSection.tsx exports ContactSection component with "use client"
    - ContactSection wraps content in SceneSection with id="contact"
    - Card container has rounded-2xl, shadow-lg, centered layout
    - Contains at least 3 contact links: Email, GitHub, LinkedIn
    - Each link has MDiv with whileHover for Ghibli-themed hover animation per D-19
    - Links use anchor tags with appropriate href values
    - External links have target="_blank" rel="noopener noreferrer"
    - Card is wrapped in MotionWrapper for scroll-entry animation
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>ContactSection renders a centered card with email, GitHub, and LinkedIn icon links, each with Ghibli-themed hover animations (scale + rotate). Zero TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contact links → external sites | Outbound navigation only, no user data sent |
| Timeline data → DOM | Static TypeScript data, no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing | Contact links | accept | Links are hardcoded to developer's own profiles |
| T-04-06 | Information Disclosure | Email address | accept | Email in contact section is intentionally public |
| T-04-07 | Tampering | External link targets | mitigate | All external links use rel="noopener noreferrer" to prevent reverse tabnabbing |
</threat_model>

<verification>
- npx tsc --noEmit passes with zero errors
- All 3 component files exist with correct exports
- Timeline has alternating layout on desktop
- Contact links have hover animations
</verification>

<success_criteria>
- AboutSection presents personal narrative and alternating vertical timeline with sequential scroll animations
- ContactSection displays centered card with animated icon links
- All components follow established patterns (use client, MotionWrapper, SceneSection, motion/react-m)
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-sections/04-03-SUMMARY.md`
</output>

---
phase: 04-content-sections
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/sections/ProjectCard.tsx
  - src/components/sections/ProjectsSection.tsx
  - src/components/sections/SkillItem.tsx
  - src/components/sections/SkillsSection.tsx
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-04, SKIL-01, SKIL-02, SKIL-03]

must_haves:
  truths:
    - "Projects section displays project cards in a responsive grid (1 col mobile, 2 col tablet, 3 col desktop)"
    - "Each project card shows a thumbnail area, title, description, and tech tags as pills"
    - "Project cards animate on scroll entry via MotionWrapper and respond to hover with scale + shadow"
    - "Skills section displays 5 category groups with headers and icon grids"
    - "Skill items animate on scroll entry with stagger per category group"
  artifacts:
    - path: "src/components/sections/ProjectCard.tsx"
      provides: "Individual project card with thumbnail, title, description, tech tags, hover animation"
      exports: ["ProjectCard"]
    - path: "src/components/sections/ProjectsSection.tsx"
      provides: "Full projects section with responsive grid of ProjectCard components"
      exports: ["ProjectsSection"]
    - path: "src/components/sections/SkillItem.tsx"
      provides: "Individual skill item with icon and name"
      exports: ["SkillItem"]
    - path: "src/components/sections/SkillsSection.tsx"
      provides: "Full skills section with categorized groups of SkillItem components"
      exports: ["SkillsSection"]
  key_links:
    - from: "src/components/sections/ProjectsSection.tsx"
      to: "src/data/projects.ts"
      via: "import { projects }"
      pattern: "import.*projects.*from.*data/projects"
    - from: "src/components/sections/ProjectCard.tsx"
      to: "src/data/projects.ts"
      via: "import { Project }"
      pattern: "import.*Project.*from.*data/projects"
    - from: "src/components/sections/SkillsSection.tsx"
      to: "src/data/skills.ts"
      via: "import { skillCategories }"
      pattern: "import.*skillCategories.*from.*data/skills"
    - from: "src/components/sections/ProjectCard.tsx"
      to: "src/components/animation/MotionWrapper.tsx"
      via: "MotionWrapper wrapping card"
      pattern: "MotionWrapper"
---

<objective>
Build the Projects and Skills content sections as composable client components.

Purpose: These are the two main portfolio showcase sections — projects demonstrate what the developer has built, skills show what technologies they command. Both use the data files from Plan 01 and the animation infrastructure from Phases 2-3.

Output: Four component files — ProjectCard, ProjectsSection, SkillItem, SkillsSection — ready to be wired into page.tsx in Plan 04.
</objective>

<execution_context>
@/home/jamesnguyen106/.claude/get-shit-done/workflows/execute-plan.md
@/home/jamesnguyen106/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-sections/04-CONTEXT.md
@.planning/phases/04-content-sections/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/data/projects.ts (created in Plan 01):
export interface Project {
  slug: string;
  title: string;
  description: string;
  techTags: string[];
  thumbnail: string;
  githubUrl: string;
  liveUrl?: string;
  content: string;
  screenshots: string[];
}
export const projects: Project[];

From src/data/skills.ts (created in Plan 01):
export interface Skill { name: string; icon: string; }
export interface SkillCategory { name: string; slug: string; skills: Skill[]; }
export const skillCategories: SkillCategory[];

<!-- Existing infrastructure contracts -->

From src/components/animation/MotionWrapper.tsx:
type MotionWrapperProps = {
  children: ReactNode; className?: string; variant?: Variants; once?: boolean;
};
export function MotionWrapper(props: MotionWrapperProps): JSX.Element;

From src/components/sections/SceneSection.tsx:
type SceneSectionProps = { children: ReactNode; className?: string; id?: string; };
export function SceneSection(props: SceneSectionProps): JSX.Element;

From src/lib/animation/variants.ts:
export const fadeInUp: Variants;
export const fadeIn: Variants;
export const scaleIn: Variants;

From src/hooks/useReducedMotion.ts:
export function useReducedMotion(): boolean;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProjectCard component and ProjectsSection</name>
  <files>src/components/sections/ProjectCard.tsx, src/components/sections/ProjectsSection.tsx</files>
  <read_first>
    - src/data/projects.ts (Plan 01 output — source of Project type and projects array)
    - src/components/animation/MotionWrapper.tsx (wrapping pattern for scroll-entry animation)
    - src/components/sections/SceneSection.tsx (wrapping pattern for section-level animation)
    - src/lib/animation/variants.ts (available animation variants: fadeInUp, scaleIn)
    - .planning/phases/04-content-sections/04-CONTEXT.md (for D-01, D-02, D-03, D-26, D-27)
  </read_first>
  <action>
    **src/components/sections/ProjectCard.tsx** (per D-02, D-03, D-27):
    - "use client" directive
    - Import Project type from @/data/projects
    - Import MotionWrapper from @/components/animation/MotionWrapper
    - Import div as MDiv from "motion/react-m" for hover animation
    - Import Link from "next/link" for navigation to detail page
    - Props: { project: Project; index: number }
    - The card is wrapped in MotionWrapper with fadeInUp and once=true for scroll-entry per D-03
    - Inside MotionWrapper, use MDiv with whileHover={{ scale: 1.03, y: -4 }} and transition {{ duration: 0.3 }} for hover effect per D-03
    - Card structure inside MDiv:
      1. Thumbnail area: div with h-48, rounded-t-2xl, overflow-hidden. Render the thumbnail as a div with style={{ background: project.thumbnail }} covering the full area (placeholder gradient). Apply a className of w-full h-full.
      2. Content area: div with p-5 space-y-3
      3. Title: h3 with font-heading text-xl text-foreground
      4. Description: p with text-sm text-foreground/70 line-clamp-3
      5. Tech tags: div flex flex-wrap gap-2, each tag is a span with text-xs px-2 py-1 rounded-full bg-primary/10 text-primary font-medium
    - Wrap the entire card (MotionWrapper outer) in a Next.js Link to /projects/{project.slug}
    - Card container (the MDiv): rounded-2xl bg-ghibli-cream shadow-md hover:shadow-xl transition-shadow overflow-hidden border border-warm/30 cursor-pointer per D-27

    **src/components/sections/ProjectsSection.tsx** (per D-01, D-26):
    - "use client" directive
    - Import projects from @/data/projects
    - Import ProjectCard from ./ProjectCard
    - Import SceneSection from @/components/sections/SceneSection
    - Render a SceneSection with id="projects" and className="min-h-screen px-6 py-20 sm:px-10 lg:px-16" per D-26
    - Section heading: h2 with font-heading text-4xl sm:text-5xl text-primary text-center mb-4
    - Section subtitle: p with text-foreground/60 text-center mb-12 max-w-2xl mx-auto
    - Grid: div with grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto per D-01
    - Map over projects array and render ProjectCard for each, passing project and index as props, key={project.slug}
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/ProjectCard.tsx exports ProjectCard component with "use client"
    - ProjectCard imports Project type from @/data/projects
    - ProjectCard is wrapped in MotionWrapper for scroll-entry animation
    - ProjectCard contains MDiv with whileHover for hover animation
    - ProjectCard renders thumbnail div, h3 title, p description, and tech tag spans
    - ProjectCard is wrapped in Link to /projects/{slug}
    - src/components/sections/ProjectsSection.tsx exports ProjectsSection component
    - ProjectsSection wraps content in SceneSection with id="projects"
    - Grid has classes grid-cols-1 md:grid-cols-2 lg:grid-cols-3
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>ProjectCard renders with thumbnail, title, description, tech tags, scroll-entry animation, and hover interaction. ProjectsSection displays a responsive grid of all project cards. Zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build SkillItem component and SkillsSection</name>
  <files>src/components/sections/SkillItem.tsx, src/components/sections/SkillsSection.tsx</files>
  <read_first>
    - src/data/skills.ts (Plan 01 output — source of Skill, SkillCategory, skillCategories)
    - src/components/animation/MotionWrapper.tsx (wrapping pattern)
    - src/components/sections/SceneSection.tsx (section wrapper)
    - src/lib/animation/variants.ts (for fadeInUp, scaleIn variants)
    - .planning/phases/04-content-sections/04-CONTEXT.md (for D-08, D-10, D-12, D-26, D-28)
  </read_first>
  <action>
    **src/components/sections/SkillItem.tsx** (per D-10):
    - "use client" directive
    - Import Skill type from @/data/skills
    - Import div as MDiv from "motion/react-m" for hover animation
    - Props: { skill: Skill }
    - Render a MDiv with whileHover={{ scale: 1.08, y: -2 }} transition={{ duration: 0.2 }}
    - Container: flex flex-col items-center gap-2 p-4 rounded-xl bg-background/80 border border-warm/20 shadow-sm
    - Icon: span with text-2xl (renders skill.icon emoji)
    - Name: span with text-sm font-medium text-foreground/80 text-center

    **src/components/sections/SkillsSection.tsx** (per D-08, D-09, D-12, D-26):
    - "use client" directive
    - Import skillCategories from @/data/skills
    - Import SkillItem from ./SkillItem
    - Import SceneSection from @/components/sections/SceneSection
    - Import MotionWrapper from @/components/animation/MotionWrapper
    - Import fadeInUp from @/lib/animation/variants
    - Render a SceneSection with id="skills" and className="min-h-screen px-6 py-20 sm:px-10 lg:px-16"
    - Section heading: h2 with font-heading text-4xl sm:text-5xl text-primary text-center mb-4
    - Section subtitle: p with text-foreground/60 text-center mb-12 max-w-2xl mx-auto
    - Container: div with max-w-5xl mx-auto space-y-12
    - For each category in skillCategories, render a MotionWrapper (for stagger per group, per D-12) containing:
      1. Category heading: h3 with font-heading text-2xl text-secondary mb-6
      2. Grid of SkillItem components: div with grid grid-cols-3 sm:grid-cols-4 md:grid-cols-5 lg:grid-cols-6 gap-4
      3. Map category.skills to SkillItem components, key={skill.name}
    - Each category MotionWrapper uses fadeInUp variant with once=true for staggered scroll-entry
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/SkillItem.tsx exports SkillItem component with "use client"
    - SkillItem imports Skill type from @/data/skills
    - SkillItem renders emoji icon and skill name
    - SkillItem has MDiv with whileHover for hover animation
    - src/components/sections/SkillsSection.tsx exports SkillsSection component
    - SkillsSection wraps content in SceneSection with id="skills"
    - SkillsSection renders all 5 categories from skillCategories
    - Each category group is wrapped in MotionWrapper for stagger scroll-entry
    - Each category has an h3 heading and a grid of SkillItem components
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>SkillItem renders icon + name with hover animation. SkillsSection displays 5 category groups with headings, icon grids, and staggered scroll-entry animations. Zero TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data files → Components | Static typed data arrays, no user input |
| Link href → Next.js router | Hardcoded slug-based URLs from typed data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | Project data | accept | Data is static TypeScript, not user-editable at runtime |
| T-04-04 | Information Disclosure | Project URLs | accept | All project URLs are intentionally public |
</threat_model>

<verification>
- npx tsc --noEmit passes with zero errors
- All 4 component files exist with correct exports
- ProjectCard and SkillItem both have Motion hover interactions
- ProjectsSection uses responsive grid classes
- SkillsSection renders 5 category groups
</verification>

<success_criteria>
- ProjectsSection renders a responsive grid of animated project cards with thumbnail, title, description, and tech tags
- SkillsSection renders 5 categorized skill groups with icon grids and scroll-entry animations
- All components follow established patterns (use client, MotionWrapper, SceneSection, motion/react-m)
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-sections/04-02-SUMMARY.md`
</output>

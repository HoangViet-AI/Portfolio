---
phase: 04-content-sections
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/app/projects/[slug]/page.tsx
autonomous: true
requirements: [PROJ-03]

must_haves:
  truths:
    - "Home page renders all 4 content sections (Projects, Skills, About, Contact) in order after the hero scene"
    - "Navbar is visible on every page via layout.tsx integration"
    - "Each project has a detail page at /projects/[slug] with full description, tech stack, links, and back navigation"
    - "Project detail pages are statically generated via generateStaticParams for all project slugs"
    - "npm run build succeeds with static export generating all pages including project detail routes"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home page composing HeroScene, ProjectsSection, SkillsSection, AboutSection, ContactSection"
      exports: ["default Home"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with Navbar integrated above children"
      exports: ["default RootLayout"]
    - path: "src/app/projects/[slug]/page.tsx"
      provides: "Project detail page with generateStaticParams for static export"
      exports: ["default ProjectDetailPage", "generateStaticParams"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/ProjectsSection.tsx"
      via: "import { ProjectsSection }"
      pattern: "ProjectsSection"
    - from: "src/app/page.tsx"
      to: "src/components/sections/SkillsSection.tsx"
      via: "import { SkillsSection }"
      pattern: "SkillsSection"
    - from: "src/app/page.tsx"
      to: "src/components/sections/AboutSection.tsx"
      via: "import { AboutSection }"
      pattern: "AboutSection"
    - from: "src/app/page.tsx"
      to: "src/components/sections/ContactSection.tsx"
      via: "import { ContactSection }"
      pattern: "ContactSection"
    - from: "src/app/projects/[slug]/page.tsx"
      to: "src/data/projects.ts"
      via: "import { projects }"
      pattern: "import.*projects.*from.*data/projects"
    - from: "src/app/layout.tsx"
      to: "src/components/navigation/Navbar.tsx"
      via: "import { Navbar }"
      pattern: "Navbar"
---

<objective>
Wire all content sections into the home page, integrate the Navbar into the root layout, and create project detail pages with static generation.

Purpose: This is the final assembly plan — connecting all components built in Plans 01-03 into a working site. The home page becomes a complete scrollable portfolio, the Navbar enables navigation, and project detail pages provide deep-dive content with shareable URLs.

Output: Updated page.tsx and layout.tsx, plus a new dynamic route for project details. The site builds successfully with all routes statically exported.
</objective>

<execution_context>
@/home/jamesnguyen106/.claude/get-shit-done/workflows/execute-plan.md
@/home/jamesnguyen106/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-sections/04-CONTEXT.md
@.planning/phases/04-content-sections/04-01-SUMMARY.md
@.planning/phases/04-content-sections/04-02-SUMMARY.md
@.planning/phases/04-content-sections/04-03-SUMMARY.md

<interfaces>
<!-- Section components from Plans 02 and 03 -->

From src/components/sections/ProjectsSection.tsx (Plan 02):
export function ProjectsSection(): JSX.Element;
// Renders SceneSection id="projects" with responsive grid of ProjectCards

From src/components/sections/SkillsSection.tsx (Plan 02):
export function SkillsSection(): JSX.Element;
// Renders SceneSection id="skills" with categorized skill groups

From src/components/sections/AboutSection.tsx (Plan 03):
export function AboutSection(): JSX.Element;
// Renders SceneSection id="about" with narrative + timeline

From src/components/sections/ContactSection.tsx (Plan 03):
export function ContactSection(): JSX.Element;
// Renders SceneSection id="contact" with icon links

From src/components/navigation/Navbar.tsx (Plan 01):
export function Navbar(): JSX.Element;
// Fixed navbar with scroll-aware bg, active section, mobile hamburger

<!-- Data contracts for project detail page -->

From src/data/projects.ts (Plan 01):
export interface Project {
  slug: string;
  title: string;
  description: string;
  techTags: string[];
  thumbnail: string;
  githubUrl: string;
  liveUrl?: string;
  content: string;
  screenshots: string[];
}
export const projects: Project[];

<!-- Existing contracts -->

From src/components/hero/HeroScene.tsx:
export function HeroScene(): JSX.Element;

From src/components/providers/AnimationProvider.tsx:
export function AnimationProvider({ children }: { children: ReactNode }): JSX.Element;

From next.config.ts:
// output: "export", basePath: isProd ? "/Portfolio" : "", trailingSlash: true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire all sections into page.tsx and integrate Navbar into layout.tsx</name>
  <files>src/app/page.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/app/page.tsx (current state — has HeroScene + placeholder SceneSections to replace)
    - src/app/layout.tsx (current state — has AnimationProvider, font variables, metadata)
    - src/components/hero/HeroScene.tsx (verify import path)
    - .planning/phases/04-content-sections/04-CONTEXT.md (for D-26 — all sections in SceneSection)
  </read_first>
  <action>
    **src/app/page.tsx:**
    - This is a server component (no "use client" directive). Keep it as a server component.
    - Remove the two placeholder SceneSection imports and placeholder content
    - Import ProjectsSection from @/components/sections/ProjectsSection
    - Import SkillsSection from @/components/sections/SkillsSection
    - Import AboutSection from @/components/sections/AboutSection
    - Import ContactSection from @/components/sections/ContactSection
    - Keep existing HeroScene import
    - Remove SceneSection import (no longer used directly in page.tsx)
    - Render inside main (keep className="overflow-x-hidden"):
      1. HeroScene (unchanged)
      2. ProjectsSection
      3. SkillsSection
      4. AboutSection
      5. ContactSection
    - Add a footer element after ContactSection: footer with text-center py-8 text-foreground/40 text-sm containing a copyright line and "Built with a Ghibli spirit" tagline

    **src/app/layout.tsx:**
    - Import Navbar from @/components/navigation/Navbar
    - Place Navbar inside the body tag, before AnimationProvider children — so it appears above all page content
    - The Navbar is a client component, so it can be placed inside the server layout as a component import
    - Keep all existing code (metadata, font variables, AnimationProvider) unchanged
    - Structure: body > Navbar + AnimationProvider > children
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx imports and renders ProjectsSection, SkillsSection, AboutSection, ContactSection (in that order after HeroScene)
    - src/app/page.tsx does NOT have "use client" directive (remains server component)
    - src/app/page.tsx no longer imports SceneSection directly or renders placeholder content
    - src/app/page.tsx has a footer element with copyright text
    - src/app/layout.tsx imports and renders Navbar inside body tag
    - Navbar renders before/outside AnimationProvider children
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>Home page renders hero + 4 content sections + footer. Navbar is visible on all pages via root layout. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create project detail pages with generateStaticParams</name>
  <files>src/app/projects/[slug]/page.tsx</files>
  <read_first>
    - src/data/projects.ts (Plan 01 output — Project type and projects array for generateStaticParams and content)
    - next.config.ts (output: "export", basePath, trailingSlash settings that affect routing)
    - src/components/animation/MotionWrapper.tsx (for scroll-entry animation on detail page)
    - src/lib/animation/variants.ts (for fadeInUp variant)
    - .planning/phases/04-content-sections/04-CONTEXT.md (for D-04, D-05)
  </read_first>
  <action>
    **src/app/projects/[slug]/page.tsx** (per D-04, D-05):
    - This is a server component (no "use client" — generateStaticParams requires server component)
    - Import projects and Project type from @/data/projects
    - Import Link from "next/link"
    - Import Metadata from "next" for generateMetadata
    - Import a client wrapper component for animations (create an inline or separate ProjectDetailContent client component within the same file or as a separate import — if inline, add a "use client" boundary via a separate component file)

    Since this page needs animations but generateStaticParams requires a server component, structure as follows:
    - The page.tsx file itself is a server component
    - Create a ProjectDetailClient component inline or import from a separate file. For simplicity, define the page component to find the project by slug, pass data to a client child.
    - Actually, for the simplest approach: keep page.tsx as server component that does data lookup and renders static HTML. Use MotionWrapper only via a small client wrapper if needed — OR just render static content without client-side animations on the detail page (detail pages are content-focused, not animation-heavy).

    Chosen approach — server component with static rendering:
    - Export async function generateStaticParams(): return projects.map(p => ({ slug: p.slug }))
    - Export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): resolve params, find project by slug, return { title: project.title + " | AI Engineer Portfolio", description: project.description }
    - Default export: async function ProjectDetailPage({ params }: { params: Promise<{ slug: string }> })
    - Resolve params with await, find project by slug from projects array
    - If project not found, call notFound() from "next/navigation"
    - Page layout per D-05:
      1. Back link: Link to="/" with className="inline-flex items-center gap-2 text-primary hover:text-primary/80 mb-8" — text "Back to Portfolio" with a left arrow character
      2. Hero banner: div with h-64 sm:h-80 rounded-2xl overflow-hidden mb-8, inner div with style={{ background: project.thumbnail }} w-full h-full (gradient placeholder)
      3. Title: h1 with font-heading text-4xl sm:text-5xl text-foreground mb-4
      4. Tech stack: div flex flex-wrap gap-2 mb-8, each tag as span with px-3 py-1 rounded-full bg-primary/10 text-primary text-sm font-medium
      5. Links row: div flex gap-4 mb-8. GitHub link as anchor with className including px-4 py-2 rounded-lg bg-primary text-background font-medium hover:bg-primary/90. If liveUrl exists, render a "Live Demo" link with similar styling but bg-secondary.
      6. Content: div with prose prose-lg max-w-none text-foreground/80 leading-relaxed — render project.content as paragraphs (split by newline if needed, or just render in a single div)
    - Wrap the entire content in a main with max-w-4xl mx-auto px-6 py-12 sm:px-10 pt-24 (pt-24 accounts for fixed navbar height)
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/projects/[slug]/page.tsx exists and exports default function, generateStaticParams, and generateMetadata
    - generateStaticParams returns an array of { slug } objects for all projects in the data file
    - Page finds project by slug and calls notFound() if not found
    - Page renders: back link to "/", hero banner with thumbnail gradient, h1 title, tech tags, GitHub link, content paragraphs
    - If project has liveUrl, a "Live Demo" link is also rendered
    - Page has pt-24 or equivalent top padding to account for fixed navbar
    - npm run build succeeds and generates static pages for all project slugs (check output shows /projects/shrimp-detection, /projects/sentiment-analyzer, etc.)
    - Build output shows all routes prerendered successfully
  </acceptance_criteria>
  <done>Project detail pages are statically generated for all project slugs. Each page shows hero banner, title, tech stack, links, and full content. npm run build succeeds with all routes exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → page render | Slug from URL used to look up static data — validated by generateStaticParams |
| External links on detail pages | GitHub/demo URLs from static data — outbound only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | URL slug param | mitigate | generateStaticParams ensures only valid slugs produce pages; invalid slugs hit notFound() |
| T-04-09 | Spoofing | External project links | accept | Links point to developer's own repos — static data not user-editable |
</threat_model>

<verification>
- npx tsc --noEmit passes with zero errors
- npm run build succeeds with static export
- Build output shows home page + all project detail pages generated
- All section IDs present in home page: projects, skills, about, contact
- Navbar visible on all pages
</verification>

<success_criteria>
- Home page is a complete scrollable portfolio with hero + projects + skills + about + contact + footer
- Navbar is integrated in layout.tsx and visible on every page
- Each project has a detail page at /projects/[slug] with full content and navigation back to home
- All pages are statically generated — npm run build produces the complete out/ directory
- Zero TypeScript errors, successful build
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-sections/04-04-SUMMARY.md`
</output>

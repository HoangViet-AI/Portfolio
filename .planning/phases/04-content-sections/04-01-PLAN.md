---
phase: 04-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/projects.ts
  - src/data/skills.ts
  - src/data/timeline.ts
  - src/components/navigation/Navbar.tsx
  - src/hooks/useActiveSection.ts
autonomous: true
requirements: [NAVI-01, NAVI-02, NAVI-03]

must_haves:
  truths:
    - "Project data is typed and exports an array of 4 AI/ML projects with slug, title, description, techTags, thumbnail, githubUrl, liveUrl, and content fields"
    - "Skills data is typed and exports 5 categories (ML/DL, CV, NLP, Tools, Languages) each with an array of skill items"
    - "Timeline data is typed and exports an array of career milestone entries with year, title, description fields"
    - "Fixed navbar is visible, transitions from transparent to solid on scroll, highlights active section, and has mobile hamburger menu"
  artifacts:
    - path: "src/data/projects.ts"
      provides: "Typed project data array with 4 placeholder AI/ML projects"
      exports: ["Project", "projects"]
    - path: "src/data/skills.ts"
      provides: "Typed skills data grouped by 5 categories"
      exports: ["Skill", "SkillCategory", "skillCategories"]
    - path: "src/data/timeline.ts"
      provides: "Typed timeline entry data"
      exports: ["TimelineEntry", "timelineEntries"]
    - path: "src/components/navigation/Navbar.tsx"
      provides: "Fixed top navbar with scroll-aware background, smooth scrolling, active section highlighting, and mobile hamburger"
      exports: ["Navbar"]
    - path: "src/hooks/useActiveSection.ts"
      provides: "Intersection Observer hook that tracks which section is currently in the viewport"
      exports: ["useActiveSection"]
  key_links:
    - from: "src/components/navigation/Navbar.tsx"
      to: "src/hooks/useActiveSection.ts"
      via: "hook import"
      pattern: "useActiveSection"
---

<objective>
Create all data files (projects, skills, timeline) and the navigation component with full functionality.

Purpose: Data files are the foundation that all content sections consume. Navigation is independent of content but must be built early so it can be wired into the layout. Building these first unblocks all Wave 2 section plans.

Output: Three typed data modules in src/data/ and a fully functional Navbar component with mobile support.
</objective>

<execution_context>
@/home/jamesnguyen106/.claude/get-shit-done/workflows/execute-plan.md
@/home/jamesnguyen106/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-sections/04-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/design-tokens.ts:
export const colors = {
  ghibliGreen: "#7BAE7F", ghibliPink: "#F4A7BB", ghibliSky: "#87CEEB",
  ghibliWheat: "#F5DEB3", ghibliCream: "#FAFAF5", ghibliInk: "#3B3B3B",
} as const;
export const zIndex = {
  background: 0, sceneBg: 10, sceneMid: 20, sceneFg: 30,
  content: 40, particles: 50, nav: 100, modal: 200,
} as const;

From src/lib/animation/variants.ts:
export const fadeInUp: Variants;
export const fadeIn: Variants;
export const scaleIn: Variants;
export const slideInLeft: Variants;
export const slideInRight: Variants;
export const hoverScale: { whileHover: ..., whileTap: ... };

From src/hooks/useReducedMotion.ts:
export function useReducedMotion(): boolean;

From src/hooks/useMediaQuery.ts:
export function useMediaQuery(query: string): boolean;

From src/components/animation/MotionWrapper.tsx:
type MotionWrapperProps = {
  children: ReactNode; className?: string; variant?: Variants; once?: boolean;
};
export function MotionWrapper(props: MotionWrapperProps): JSX.Element;

From src/app/globals.css:
--z-index-nav: 100;
--ease-ghibli: cubic-bezier(0.22, 1, 0.36, 1);

From src/app/fonts.ts:
// Patrick Hand (heading), Noto Sans (body) — applied via CSS variables
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed data files for projects, skills, and timeline</name>
  <files>src/data/projects.ts, src/data/skills.ts, src/data/timeline.ts</files>
  <read_first>
    - src/lib/design-tokens.ts (for color naming conventions)
    - src/app/page.tsx (for current section IDs: "projects", "about")
    - .planning/phases/04-content-sections/04-CONTEXT.md (for D-06, D-07, D-09, D-10, D-11, D-15, D-17)
  </read_first>
  <action>
    Create three data files in src/data/:

    **src/data/projects.ts** (per D-06, D-07):
    - Export a Project interface with fields: slug (string), title (string), description (string), techTags (string[]), thumbnail (string — path or gradient placeholder), githubUrl (string), liveUrl (string, optional), content (string — longer markdown-ish description for detail page), screenshots (string[] — placeholder paths)
    - Export a const projects array with 4 items. Use AI/ML themed projects per D-07: "shrimp-detection" (Shrimp Detection System — YOLOv8, Python, OpenCV), "sentiment-analyzer" (NLP Sentiment Analyzer — BERT, PyTorch, FastAPI), "style-transfer" (Neural Style Transfer — TensorFlow, CNN, Flask), "rag-chatbot" (RAG Chatbot — LangChain, OpenAI, Next.js). Each with 2-4 sentence description, 3-5 techTags, placeholder thumbnail as CSS gradient string (e.g. "linear-gradient(135deg, #7bae7f 0%, #87ceeb 100%)"), realistic githubUrl pattern "https://github.com/username/repo", a content field with 2-3 paragraphs, and empty screenshots array.

    **src/data/skills.ts** (per D-09, D-10, D-11):
    - Export a Skill interface with fields: name (string), icon (string — emoji placeholder)
    - Export a SkillCategory interface with fields: name (string), slug (string), skills (Skill[])
    - Export a const skillCategories array with 5 categories per D-09: "ML & Deep Learning" (PyTorch, TensorFlow, scikit-learn, Keras, Hugging Face), "Computer Vision" (OpenCV, YOLOv8, MediaPipe, Pillow), "NLP" (spaCy, NLTK, LangChain, Transformers), "Tools & Frameworks" (Docker, Git, FastAPI, Next.js, PostgreSQL, MLflow), "Languages" (Python, TypeScript, SQL, Rust, C++). Each skill gets an emoji icon placeholder.

    **src/data/timeline.ts** (per D-15, D-17):
    - Export a TimelineEntry interface with fields: year (string), title (string), description (string), illustration (string, optional — placeholder path)
    - Export a const timelineEntries array with 5-6 entries representing a plausible AI engineer career journey: university CS degree, first ML internship, computer vision role, NLP research, current AI engineer position, open source contribution. Each with year, title, and 1-2 sentence description.
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/data/projects.ts exports interface Project and const projects with exactly 4 items
    - src/data/skills.ts exports interface Skill, SkillCategory and const skillCategories with exactly 5 categories
    - src/data/timeline.ts exports interface TimelineEntry and const timelineEntries with 5-6 items
    - Each project has all required fields: slug, title, description, techTags, thumbnail, githubUrl, content
    - Each skill category has name, slug, skills array with at least 3 skills each
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>Three data files exist with correct types, realistic placeholder data, and zero TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Build Navbar with scroll-aware background, active section highlighting, and mobile hamburger menu</name>
  <files>src/hooks/useActiveSection.ts, src/components/navigation/Navbar.tsx</files>
  <read_first>
    - src/hooks/useMediaQuery.ts (for hook pattern to follow)
    - src/hooks/useReducedMotion.ts (for hook pattern)
    - src/components/animation/MotionWrapper.tsx (for motion/react-m import pattern)
    - src/lib/animation/variants.ts (for available variants)
    - src/app/globals.css (for z-index-nav: 100, color tokens)
    - src/app/layout.tsx (where Navbar will be integrated in Plan 04)
    - .planning/phases/04-content-sections/04-CONTEXT.md (for D-21 through D-25)
  </read_first>
  <action>
    **src/hooks/useActiveSection.ts** (per D-23):
    - "use client" directive
    - Export function useActiveSection(sectionIds: string[]): string | null
    - Uses IntersectionObserver to watch all elements with the given IDs
    - Observer options: threshold 0.3 (section must be 30% visible), rootMargin "0px 0px -20% 0px"
    - Maintains state of the currently active section ID
    - Returns the active section ID or null if none is visible
    - Cleans up observer on unmount
    - Uses useState and useEffect from React

    **src/components/navigation/Navbar.tsx** (per D-21, D-22, D-23, D-24, D-25):
    - "use client" directive
    - Import useActiveSection from the hook above
    - Import useReducedMotion, useMediaQuery from existing hooks
    - Import div as MDiv from "motion/react-m" for animations (matching MotionWrapper import pattern)
    - Import AnimatePresence from "motion/react" for menu open/close
    - Navigation links: Projects (#projects), Skills (#skills), About (#about), Contact (#contact)
    - Fixed positioning with z-index var(--z-index-nav) per D-25
    - Scroll-aware background per D-21: track window.scrollY via useEffect + scroll listener. When scrollY > 50, apply bg-background/95 backdrop-blur-sm with transition. When scrollY <= 50, transparent background.
    - Smooth scroll per D-22: onClick handler calls document.getElementById(sectionId)?.scrollIntoView({ behavior: "smooth" }) and prevents default. When reduced motion is active, use behavior: "auto" instead.
    - Active section per D-23: call useActiveSection(["projects", "skills", "about", "contact"]). Active link gets text-primary and a bottom border or underline indicator.
    - Desktop layout: horizontal link row with site title/logo on the left
    - Mobile layout per D-24: hamburger button (three-line icon) visible below md breakpoint (use useMediaQuery("(min-width: 768px)")). Toggling hamburger opens a full-screen overlay with vertical nav links. Use MDiv with fadeIn variant for overlay entrance. Clicking a link closes the menu.
    - Navbar height: h-16 (4rem) for consistent spacing
    - Use Tailwind classes only, no inline styles except z-index CSS variable
  </action>
  <verify>
    <automated>cd /home/jamesnguyen106/Downloads/Portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useActiveSection.ts exports useActiveSection function accepting string[] and returning string | null
    - src/components/navigation/Navbar.tsx exports Navbar component with "use client" directive
    - Navbar contains fixed positioning with z-[var(--z-index-nav)] or equivalent z-100
    - Navbar has 4 nav links: Projects, Skills, About, Contact
    - Scroll listener toggles background class at scrollY > 50 threshold
    - useActiveSection is called with ["projects", "skills", "about", "contact"]
    - Active link receives text-primary styling
    - Mobile hamburger button exists with md:hidden class
    - Mobile overlay uses MDiv or AnimatePresence for animation
    - scrollIntoView is called with behavior "smooth" (or "auto" for reduced motion)
    - npx tsc --noEmit passes with zero errors
  </acceptance_criteria>
  <done>Navbar component renders with scroll-aware background transition, active section highlighting via IntersectionObserver, smooth scroll navigation, and animated mobile hamburger menu. TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Data files → Components | Static typed data, no user input — no trust boundary crossed |
| Nav links → DOM scroll | Only scrolls to hardcoded section IDs — no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Nav links | accept | Links are hardcoded anchors, not user-provided URLs |
| T-04-02 | Information Disclosure | Project data | accept | All project data is intentionally public portfolio content |
</threat_model>

<verification>
- npx tsc --noEmit passes with zero errors
- All 5 files exist: 3 data files + Navbar + useActiveSection hook
- Data files export correct types and arrays
- Navbar component is a client component with all required functionality
</verification>

<success_criteria>
- Three data modules provide typed, importable data for all content sections
- Navbar is fully functional: transparent→solid scroll transition, active section tracking, mobile hamburger, smooth scroll
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-sections/04-01-SUMMARY.md`
</output>
